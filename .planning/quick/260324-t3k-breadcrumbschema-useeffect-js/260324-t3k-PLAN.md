---
phase: quick-260324-t3k
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/BreadcrumbSchema.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "BreadcrumbSchema JSON-LD is present in initial HTML response (not injected via JS)"
  artifacts:
    - path: app/components/BreadcrumbSchema.tsx
      contains: "dangerouslySetInnerHTML"
  key_links:
    - from: app/components/BreadcrumbSchema.tsx
      to: HTML
      via: inline script tag
---

<objective>
Convert BreadcrumbSchema from client-side useEffect JS injection to server-rendered inline script.
</objective>

<context>
@app/components/BreadcrumbSchema.tsx

**Reference (already completed similar task):**
From 260324-sye-SUMMARY.md — ArticleSchema conversion pattern:
- Remove 'use client' directive
- Remove useEffect
- Use inline `<script type="application/ld+json">` with `dangerouslySetInnerHTML`
</context>

<tasks>

<task type="auto">
  <name>Convert BreadcrumbSchema to server component</name>
  <files>app/components/BreadcrumbSchema.tsx</files>
  <action>
    Convert BreadcrumbSchema.tsx from client component to server component:

    1. Remove `'use client'` directive
    2. Remove `import { useEffect } from 'react'`
    3. Remove the `useEffect` block that creates and appends script
    4. Return an inline `<script type="application/ld+json">` tag with `dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }}`

    The component should render the JSON-LD schema directly into the HTML as an inline script tag, not inject it via JavaScript after mount.
  </action>
  <verify>
    <automated>grep -c "use client" app/components/BreadcrumbSchema.tsx && grep -c "useEffect" app/components/BreadcrumbSchema.tsx && grep -c "dangerouslySetInnerHTML" app/components/BreadcrumbSchema.tsx</automated>
  </verify>
  <done>BreadcrumbSchema uses dangerouslySetInnerHTML inline script, no useEffect, no use client</done>
</task>

</tasks>

<verification>
- grep "use client" app/components/BreadcrumbSchema.tsx returns 0
- grep "useEffect" app/components/BreadcrumbSchema.tsx returns 0
- grep "dangerouslySetInnerHTML" app/components/BreadcrumbSchema.tsx returns 1
- npm run build passes
</verification>

<success_criteria>
1. BreadcrumbSchema.tsx has no 'use client' directive
2. BreadcrumbSchema.tsx has no useEffect
3. BreadcrumbSchema.tsx uses dangerouslySetInnerHTML for inline JSON-LD script
4. npm run build passes
</success_criteria>

<output>
After completion, create `.planning/quick/260324-t3k-breadcrumbschema-useeffect-js/260324-t3k-SUMMARY.md`
</output>
