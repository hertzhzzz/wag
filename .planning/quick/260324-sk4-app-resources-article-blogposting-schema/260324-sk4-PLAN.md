---
phase: quick
plan: "260324-sk4"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/ArticleSchema.tsx
autonomous: true
requirements:
  - sk4
must_haves:
  truths:
    - "Blog posts have BlogPosting schema for SEO rich results"
  artifacts:
    - path: app/components/ArticleSchema.tsx
      provides: Article + BlogPosting schema output
      contains: "@type"
---

<objective>
Add BlogPosting schema type to all blog posts under /app/resources for enhanced SEO rich results.

Purpose: Google Search Console recommends BlogPosting schema for blog articles. Using ["Article", "BlogPosting"] array type maximizes rich result eligibility while maintaining Article compatibility.

Output: Updated ArticleSchema.tsx with dual schema type.
</objective>

<context>
@app/components/ArticleSchema.tsx
@app/resources/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Update ArticleSchema to output BlogPosting type</name>
  <files>app/components/ArticleSchema.tsx</files>
  <action>
    In ArticleSchema.tsx, change the schema object:
    1. Line 27: Change `"@type": "Article"` to `"@type": ["Article", "BlogPosting"]`
    2. This single change will apply BlogPosting schema to ALL blog posts since ArticleSchema is already used in app/resources/[slug]/page.tsx

    Keep all other fields unchanged. The existing fields (headline, author, datePublished, etc.) are all valid BlogPosting properties per schema.org.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>ArticleSchema outputs @type: ["Article", "BlogPosting"], all blog posts in /resources inherit dual schema</done>
</task>

</tasks>

<verification>
npm run build passes, npm run lint passes
</verification>

<success_criteria>
- ArticleSchema.tsx has @type: ["Article", "BlogPosting"]
- app/resources/[slug]/page.tsx already uses ArticleSchema (no changes needed)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/quick/260324-sk4-app-resources-article-blogposting-schema/260324-sk4-SUMMARY.md`
</output>
