# Quick Plan: 分析项目根目录的 skill 链接文件，找出可以移除的

## Task: 分析项目根目录的 skill 链接文件，找出可以移除的

## Analysis Summary

### 当前 Skill 结构

| 目录 | 类型 | 内容 |
|------|------|------|
| `.agents/skills/pagespeed-insights/` | 真实目录 | LICENSE.md, SKILL.md, reference.md |
| `skills/` (根目录) | 符号链接 | → `.agents/skills/pagespeed-insights` |
| `.claude/skills/` | 符号链接 | → `.agents/skills/pagespeed-insights` |
| 其他 26 个 `.*` 目录 | 符号链接 | → `.agents/skills/pagespeed-insights` |

### 可移除的目录 (只保留 Claude Code 相关)

| 目录 | 用途 | 建议 |
|------|------|------|
| `.claude/skills/` | Claude Code skill 加载 | **保留** |
| `skills/` (根目录) | /gsd:* 命令使用 | **保留** |
| `.agents/` | 真实 skill 位置 | **保留** |
| 其他所有 `.*` 目录 | 其他 AI 助手 | **移除** |

### 移除列表 (26 个目录)

```
.adal, .agent, .augment, .codebuddy, .commandcode,
.continue, .crush, .factory, .goose, .iflow,
.junie, .kilocode, .kiro, .kode, .mcpjam,
.mux, .neovate, .openhands, .pi, .pochi,
.qoder, .qwen, .roo, .trae, .vibe,
.windsurf, .zencoder
```

---

## Tasks

### Task 1: 移除不需要的 AI 助手配置目录

```bash
# 移除 26 个目录
rm -rf .adal .agent .augment .codebuddy .commandcode .continue .crush .factory .goose .iflow .junie .kilocode .kiro .kode .mcpjam .mux .neovate .openhands .pi .pochi .qoder .qwen .roo .trae .vibe .windsurf .zencoder
```

### Task 2: 验证保留的目录

```bash
# 确认保留的目录
ls -la .claude/skills/
ls -la skills/
ls -la .agents/skills/
```
