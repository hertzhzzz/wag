---
phase: 13-ui-optimization
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Navbar.tsx
  - app/components/HowItWorks.tsx
  - app/components/FAQ.tsx
  - app/components/industries/index.tsx
autonomous: true
requirements:
  - UI-B
  - UI-C
  - UI-D
  - UI-E
must_haves:
  truths:
    - "Cards have visible depth hierarchy (not flat gray borders)"
    - "Section labels feel brand-specific, not template-generated"
    - "HowItWorks step 1 and step 5 have distinguishable visual weight"
    - "Navbar CTA has meaningful gradient, not navy-to-navy"
  artifacts:
    - path: app/components/Navbar.tsx
      provides: CTA button with meaningful amber-to-navy gradient
    - path: app/components/HowItWorks.tsx
      provides: Section label with unique treatment + step hierarchy
    - path: app/components/FAQ.tsx
      provides: Cards with shadow-based depth, section label variation
    - path: app/components/industries/index.tsx
      provides: Section label variation
  key_links:
    - from: Navbar CTA
      to: enquiry page
      via: gradient button style
---

<objective>
解决 critique 报告中识别的 4 个 UI 问题：B(卡片层次) + C(Section标签) + D(HowItWorks权重) + E(Navbar渐变)
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-ui-optimization/13-CONTEXT.md
@/Users/mark/Projects/wag/app/components/Navbar.tsx
@/Users/mark/Projects/wag/app/components/HowItWorks.tsx
@/Users/mark/Projects/wag/app/components/FAQ.tsx
@/Users/mark/Projects/wag/app/components/industries/index.tsx

**Design System:**
- Primary: #0F2D5E (Navy)
- Accent: #F59E0B (Amber)
- Fonts: IBM Plex Sans, IBM Plex Serif
- Tailwind CSS

**Issue B:** FAQ.tsx line 36 uses `border-gray-200 rounded-lg` — flat, no visual hierarchy
**Issue C:** Section labels across components use identical `text-amber uppercase tracking` pattern — template feel
**Issue D:** HowItWorks 5 steps all have equal visual weight — no attention guidance
**Issue E:** Navbar.tsx line 65 CTA uses `from-[#0F2D5E] to-[#1a3d6e]` — meaningless gradient (both navy)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Navbar CTA gradient (Issue E)</name>
  <files>app/components/Navbar.tsx</files>
  <action>
Replace the meaningless `from-[#0F2D5E] to-[#1a3d6e]` gradient with a meaningful amber-to-navy gradient that creates visual distinction.

Change line 65 from:
```tsx
className="text-[13px] font-medium px-[22px] py-[9px] text-white bg-gradient-to-r from-[#0F2D5E] to-[#1a3d6e] ..."
```

To amber-to-navy gradient:
```tsx
className="text-[13px] font-medium px-[22px] py-[9px] text-white bg-gradient-to-r from-amber to-navy ..."
```

Keep shadow, hover:shadow-lg, hover:translate-y-[-1px], and transition-all.
</action>
  <verify>
grep -n "bg-gradient-to-r from-amber" app/components/Navbar.tsx
</verify>
  <done>Navbar CTA button uses amber-to-navy gradient with hover effects</done>
</task>

<task type="auto">
  <name>Task 2: Add visual hierarchy to HowItWorks steps (Issue D)</name>
  <files>app/components/HowItWorks.tsx</files>
  <action>
Create visual weight differentiation between step 1 (start) and step 5 (climax):

**Step 1 (Entry point):** Subtle, understated treatment
- Reduce icon container from `w-12 h-12` to `w-10 h-10`
- Reduce icon from `size={24}` to `size={20}`
- Use lighter background: `bg-navy/5` instead of `bg-amber/10`

**Step 5 (Climax/CTA):** Bold, prominent treatment
- Increase icon container to `w-14 h-14`
- Use icon size `size={28}`
- Use amber background: `bg-amber/15`
- Add subtle amber border: `border border-amber/20`
- Make card slightly elevated: `shadow-[0_8px_32px_rgba(245,158,11,0.12)]`

**Steps 2-4:** Keep current styling (medium weight, transition state)

The connecting line (line 80) should fade toward step 1 and intensify toward step 5. Update gradient:
```tsx
<div className="hidden md:block absolute top-16 left-[12%] right-[12%] h-px bg-gradient-to-r from-transparent via-amber/30 to-amber/60 z-0" />
```
</action>
  <verify>
grep -n "w-10 h-10\|w-14 h-14\|bg-amber/15\|border border-amber/20" app/components/HowItWorks.tsx
</verify>
  <done>Step 1 has understated styling, step 5 has elevated climax treatment</done>
</task>

<task type="auto">
  <name>Task 3: Differentiate section label styling (Issue C)</name>
  <files>app/components/HowItWorks.tsx, app/components/FAQ.tsx, app/components/industries/index.tsx</files>
  <action>
Replace the template pattern `text-amber uppercase tracking text-xs font-semibold` with brand-differentiated treatments:

**HowItWorks.tsx (line 66):** "Our Process" — use serif italic + amber
```tsx
<p className="text-amber/80 font-serif italic text-sm mb-4">
  Our Process
</p>
```

**FAQ.tsx (line 25):** "Common Questions" — use smaller caps + navy muted
```tsx
<p className="text-[11px] font-semibold uppercase tracking-[0.15em] text-navy/40 mb-3">
  Common Questions
</p>
```

**industries/index.tsx (line 118):** "Industries We Serve" — keep amber but vary tracking/font
```tsx
<p className="text-amber font-serif italic text-sm mb-3">
  Industries We Serve
</p>
```

This creates three distinct section label styles instead of identical template.
</action>
  <verify>
grep -n "font-serif italic\|text-navy/40\|tracking-\[0\.15em\]" app/components/HowItWorks.tsx app/components/FAQ.tsx app/components/industries/index.tsx
</verify>
  <done>Each section has unique label treatment: serif italic for HowItWorks, muted caps for FAQ, serif italic amber for Industries</done>
</task>

<task type="auto">
  <name>Task 4: Replace flat card borders with shadow hierarchy (Issue B)</name>
  <files>app/components/FAQ.tsx</files>
  <action>
Replace the flat `border-gray-200 rounded-lg` card styling with shadow-based depth hierarchy:

**FAQ.tsx line 36:** The accordion item container:
Change from:
```tsx
className="border border-gray-200 rounded-lg overflow-hidden"
```

To shadow-based depth:
```tsx
className="rounded-xl overflow-hidden bg-white shadow-[0_2px_12px_rgba(15,45,94,0.06)] hover:shadow-[0_4px_20px_rgba(15,45,94,0.1)] transition-shadow duration-300"
```

This removes the flat gray border and creates depth through shadows that intensify on hover. The white background provides contrast against the section background.
</action>
  <verify>
grep -n "shadow-\[0_2px_12px\|shadow-\[0_4px_20px" app/components/FAQ.tsx
</verify>
  <done>FAQ accordion items use shadow depth instead of flat gray borders</done>
</task>

</tasks>

<verification>
npm run build 2>&1 | tail -20
</verification>

<success_criteria>
- [ ] Navbar CTA uses amber-to-navy gradient (not navy-to-navy)
- [ ] HowItWorks step 1 is understated, step 5 is elevated/climax
- [ ] Section labels have 3 distinct styles (not uniform template)
- [ ] FAQ cards use shadow depth instead of gray borders
- [ ] npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-ui-optimization/13-01-SUMMARY.md`
</output>
