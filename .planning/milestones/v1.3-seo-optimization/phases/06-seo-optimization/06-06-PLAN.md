---
phase: 06-seo-optimization
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/blog/china-vs-alibaba.mdx
  - content/blog/china-sourcing-risks.mdx
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "Blog articles contain internal links to /services"
  artifacts:
    - path: "content/blog/china-vs-alibaba.mdx"
      provides: "Blog article with /services link"
      contains: "/services"
    - path: "content/blog/china-sourcing-risks.mdx"
      provides: "Blog article with /services link"
      contains: "/services"
  key_links:
    - from: "content/blog/china-vs-alibaba.mdx"
      to: "/services"
      via: "MDX link syntax"
      pattern: "\\[.*\\]\\(/services\\)"
---

<objective>
Verify and ensure blog articles contain proper internal links to /services.

Gap: User reported "没有链接到/services" (no link to /services)

Root cause analysis found the links DO exist in MDX source. This plan verifies they render correctly and are visible.
</objective>

<context>
@content/blog/china-vs-alibaba.mdx (line 47)
@content/blog/china-sourcing-risks.mdx (line 46)

Both articles contain /services links in their MDX content:

china-vs-alibaba.mdx line 47:
```
Winning Adventure Global helps Australian businesses [build direct factory relationships](/services) for better pricing and quality control.
```

The link exists in the markdown source. Need to verify it renders correctly in the browser.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify /services link exists in MDX files</name>
  <files>content/blog/china-vs-alibaba.mdx, content/blog/china-sourcing-risks.mdx</files>
  <action>
    Confirm both blog articles contain /services links:
    ```bash
    grep -n "/services" content/blog/china-vs-alibaba.mdx
    grep -n "/services" content/blog/china-sourcing-risks.mdx
    ```
</action>
<verify>
    <automated>grep -c "/services" content/blog/china-vs-alibaba.mdx content/blog/china-sourcing-risks.mdx</automated>
  </verify>
  <done>Both articles contain at least one /services link</done>
</task>

<task type="auto">
  <name>Task 2: Verify link renders in browser</name>
  <files>content/blog/china-vs-alibaba.mdx</files>
  <action>
    Start dev server and verify the /services link is rendered:
    ```bash
    npm run dev &
    sleep 5
    curl -s http://localhost:3000/resources/china-vs-alibaba | grep -o 'href="/services"'
    ```

    Should find the href="/services" link in the rendered HTML.
</action>
<verify>
    <automated>curl -s http://localhost:3000/resources/china-vs-alibaba | grep -o 'href="/services"'</automated>
  </verify>
  <done>/services link renders in the browser as clickable anchor tag</done>
</task>

<task type="auto">
  <name>Task 3: Stop dev server</name>
  <files></files>
  <action>
    Kill the dev server process to clean up:
    ```bash
    pkill -f "next dev" || true
    ```
</action>
<verify>
    <automated>pkill -f "next dev" 2>/dev/null; echo "cleaned"</automated>
  </verify>
  <done>Dev server stopped</done>
</task>

</tasks>

<verification>
- [ ] Both articles contain /services links in MDX source
- [ ] Links render as clickable anchor tags in browser
- [ ] Links are visible and accessible to users
</verification>

<success_criteria>
Blog article contains internal link to /services (truth: "Blog article contains internal link to /services")
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo-optimization/06-06-SUMMARY.md`
</output>
