---
phase: 08-security-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "npm audit returns no critical or high vulnerabilities"
    - "Next.js is upgraded to version 14.2.25+ to fix CVE-2025-29927"
    - "All packages are up to date or security updates are identified"
  artifacts:
    - path: "package.json"
      provides: "Updated dependencies with secure versions"
      contains: "next@14.2"
    - path: "package-lock.json"
      provides: "Locked dependency versions"
  key_links:
    - from: "package.json"
      to: "npm audit"
      via: "npm install"
      pattern: "npm audit.*passes"
---

<objective>
Scan and fix dependency vulnerabilities in the WAG website. Run npm audit to identify vulnerabilities and npm outdated to identify outdated packages. Upgrade Next.js to fix CVE-2025-29927 (middleware authorization bypass vulnerability affecting versions < 14.2.25).
</objective>

<context>
@package.json
@next.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm audit to identify vulnerabilities</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependencies and versions)
  </read_first>
  <action>
    Run npm audit to scan for vulnerabilities. Execute:
    ```
    npm audit
    ```
    Analyze the output for critical and high severity vulnerabilities. Document findings in commit message.
  </action>
  <acceptance_criteria>
    - npm audit returns exit code 0 (no vulnerabilities) or documents found vulnerabilities
    - Output shows vulnerability count by severity (critical/high/medium/low)
  </acceptance_criteria>
  <verify>
    <automated>npm audit --audit-level=moderate</automated>
  </verify>
  <done>npm audit completed, vulnerabilities identified and documented</done>
</task>

<task type="auto">
  <name>Task 2: Check for outdated packages with npm outdated</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (to verify current versions)
  </read_first>
  <action>
    Run npm outdated to identify packages that have newer versions available. Execute:
    ```
    npm outdated
    ```
    Identify packages that need updating, prioritizing security-related updates.
  </action>
  <acceptance_criteria>
    - npm outdated command executes successfully
    - Output shows list of outdated packages with current/wanted/latest versions
  </acceptance_criteria>
  <verify>
    <automated>npm outdated</automated>
  </verify>
  <done>npm outdated completed, packages needing updates identified</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade Next.js to fix CVE-2025-29927</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current next version: 14.2.0)
  </read_first>
  <action>
    Upgrade Next.js from 14.2.0 to 14.2.25+ to fix CVE-2025-29927 (middleware authorization bypass vulnerability). Execute:
    ```
    npm install next@14.2.25
    ```
    This fixes the critical vulnerability where attackers could bypass middleware authentication/authorization.
  </action>
  <acceptance_criteria>
    - package.json shows next@14.2.25 or later
    - npm install completes without errors
    - npm run build passes (VERIFICATION: build must succeed after upgrade)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Next.js upgraded to 14.2.25+, CVE-2025-29927 fixed, build passes</done>
</task>

</tasks>

<verification>
- Run `npm audit --audit-level=moderate` - should return 0 vulnerabilities after fixes
- Run `npm outdated` - check if other security updates are needed
- Run `npm run build` - must pass (CRITICAL: verifies upgrade didn't break build)
</verification>

<success_criteria>
1. npm audit shows no critical or high vulnerabilities
2. Next.js upgraded to 14.2.25+ to fix CVE-2025-29927
3. Build passes with updated dependencies (VERIFIED via npm run build)
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-audit/08-01-SUMMARY.md`
</output>
