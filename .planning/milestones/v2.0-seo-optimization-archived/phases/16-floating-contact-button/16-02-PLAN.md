---
phase: 16-floating-contact-button
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - app/layout.tsx
autonomous: true
requirements:
  - SEO-02

must_haves:
  truths:
    - "Floating contact button appears on all pages"
    - "Button visible in bottom-right corner regardless of scroll position"
  artifacts:
    - path: app/layout.tsx
      provides: FloatingContactButton integration point
  key_links:
    - from: app/layout.tsx
      to: app/components/FloatingContactButton.tsx
      via: import and render in body
      pattern: FloatingContactButton
---

<objective>
Integrate the FloatingContactButton component into the root layout so it appears on all pages. This is a single task - add one import and render the component.
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/layout.tsx
@.planning/phases/16-floating-contact-button/16-01-SUMMARY.md (generated after 16-01 completes)
@.planning/phases/16-floating-contact-button/16-UI-SPEC.md

**Note:** layout.tsx is a Server Component. Next.js App Router automatically handles the client component boundary when you import a 'use client' component into a Server Component.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FloatingContactButton to layout</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (current state - body structure)
  </read_first>
  <action>
Add FloatingContactButton to `app/layout.tsx`:

1. Add import at top of file (after existing imports):
```typescript
import FloatingContactButton from './components/FloatingContactButton'
```

2. Add component render inside `<body>`, after `{children}`:
```tsx
<body>
  {children}
  <FloatingContactButton />
</body>
```

No changes to existing body content or structure are needed.
</action>
  <verify>
    <automated>grep -n "FloatingContactButton" app/layout.tsx</automated>
  </verify>
  <done>FloatingContactButton renders on all pages via root layout</done>
</task>

</tasks>

<verification>
- Build passes: `npm run build`
- No TypeScript errors
- Component appears on homepage (/) and other pages
</verification>

<success_criteria>
- FloatingContactButton imported in layout.tsx
- Component renders after {children} in body
- All pages display the floating button in bottom-right corner
- npm run build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-floating-contact-button/16-02-SUMMARY.md`
</output>
