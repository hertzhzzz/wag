---
phase: 16-floating-contact-button
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/contact/route.ts
  - app/components/FloatingContactButton.tsx
  - app/globals.css
autonomous: true
requirements:
  - SEO-02

must_haves:
  truths:
    - "Floating button visible on all pages in bottom-right corner"
    - "Button has pulse ring animation that draws attention"
    - "Click opens modal with Email + Message form"
    - "Submit sends data to /api/contact and shows success state"
    - "ESC key and overlay click close the modal"
  artifacts:
    - path: app/api/contact/route.ts
      provides: Simplified contact API (email + message only)
      exports: POST handler with Zod validation
    - path: app/components/FloatingContactButton.tsx
      provides: Floating button + modal component
      exports: FloatingContactButton default export
    - path: app/globals.css
      provides: Pulse ring CSS keyframe animation
  key_links:
    - from: FloatingContactButton.tsx
      to: /api/contact
      via: fetch POST on form submit
      pattern: fetch.*api/contact.*POST
    - from: FloatingContactButton.tsx
      to: globals.css
      via: animate-pulse-ring class
      pattern: animate-pulse-ring
---

<objective>
Create the floating contact button component and its supporting API endpoint. The button will be a Navy circle with pulse ring animation in the bottom-right corner, opening a modal with Email + Message fields. This is a vertical slice - API + Component together.
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/components/DirectoryAccessModal.tsx
@app/api/enquiry/route.ts
@tailwind.config.ts
@.planning/phases/16-floating-contact-button/16-UI-SPEC.md
@.planning/phases/16-floating-contact-button/16-CONTEXT.md

**Key Design Decisions (locked):**
- Navy (#0F2D5E) button color
- Pulse ring: 64px desktop / 48px mobile, opacity 0.5-1.0, 2s cycle
- Email + Message fields only
- "Send Message" submit button
- z-index: 9999
- Modal: Navy/60 backdrop + backdrop-blur-sm, fade-in + zoom-in-95

**API Decision:** Per 16-RESEARCH.md recommendation, create new `/api/contact` endpoint to handle simplified form (email + message only) rather than modifying existing `/api/enquiry` schema.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/contact endpoint</name>
  <files>app/api/contact/route.ts</files>
  <read_first>
    - app/api/enquiry/route.ts (reference for pattern - Zod validation, CORS, error handling, email sending)
  </read_first>
  <action>
    Create `app/api/contact/route.ts` with:

**Schema (Zod):**
```typescript
const contactSchema = z.object({
  email: z.string().email('Please enter a valid email address.'),
  message: z.string().min(10, 'Message must be at least 10 characters.').max(2000, 'Message must be under 2000 characters.'),
})
```

**Implementation requirements:**
- POST handler with Zod validation
- CORS configuration matching `/api/enquiry` (ALLOWED_ORIGINS array)
- Rate limiting using `checkRateLimit` from `@/lib/rate-limit`
- Nodemailer email sending to GMAIL_USER
- HTML email template matching existing brand styling (Navy #0F2D5E header, Amber accents)
- Subject: "New Contact Form Message"
- ReplyTo: sender email
- Error handling: validation errors (400), rate limit (429), server errors (500)
- Success: { ok: true } with 200 status

**Email HTML content:**
- Header: "New Contact Form Submission" in Navy
- Fields: Email, Message
- Professional template matching existing enquiry email style
</action>
  <verify>
    <automated>grep -l "contactSchema" app/api/contact/route.ts && grep -l "z.object" app/api/contact/route.ts</automated>
  </verify>
  <done>POST /api/contact accepts {email, message} and returns {ok: true} on success</done>
</task>

<task type="auto">
  <name>Task 2: Create FloatingContactButton component</name>
  <files>app/components/FloatingContactButton.tsx, app/globals.css</files>
  <read_first>
    - app/components/DirectoryAccessModal.tsx (reference for modal patterns, ESC handling, form submission)
    - app/globals.css (existing styles)
    - .planning/phases/16-floating-contact-button/16-UI-SPEC.md (exact specifications)
  </read_first>
  <action>
**File 1: app/globals.css** — Add pulse ring keyframe before any @tailwind directives:
```css
@keyframes pulse-ring {
  0%, 100% { opacity: 0.5; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.05); }
}

.animate-pulse-ring {
  animation: pulse-ring 2s ease-in-out infinite;
}

@media (prefers-reduced-motion: reduce) {
  .animate-pulse-ring {
    animation: none;
  }
}
```

**File 2: app/components/FloatingContactButton.tsx** — Create client component with:

**Imports:** useState, useEffect from react; Mail, X from lucide-react

**State:** isOpen, email, message, submitted, loading, error

**Button (desktop: 56px, mobile: 40px):**
- Fixed position: bottom-5 right-5 (20px), z-[9999]
- Navy background, rounded-full, shadow-lg
- Mail icon: 24px desktop / 18px mobile
- "Contact Us" text below icon (hidden on mobile < sm:)
- Pulse ring: absolute, 64px desktop / 48px mobile, border-2 border-navy/50, animate-pulse-ring
- Hover: scale(1.05), shadow-xl, 150ms transition
- ARIA: aria-label="Open contact form"

**Modal overlay:**
- Fixed inset-0, z-[9999], flex items-center justify-center
- Background: absolute inset-0 bg-navy/60 backdrop-blur-sm
- Click on overlay closes modal
- ESC key listener (useEffect pattern from DirectoryAccessModal)

**Modal dialog:**
- relative bg-white rounded-2xl shadow-2xl
- Max-width: 448px (28rem), padding: 32px desktop / 24px mobile
- Animation: animate-in fade-in zoom-in-95 duration-200
- Close button (X icon): top-4 right-4, 40x40px, hover:bg-navy/5
- ARIA: role="dialog" aria-modal="true"

**Form fields:**
- Email input: type="email", placeholder="your@email.com", required
- Message textarea: placeholder="How can we help you?", rows=4, required
- Field styling: border border-navy/10, rounded-xl, focus:border-amber focus:ring-2 focus:ring-amber/20

**Submit button:**
- Label: "Send Message"
- Background: amber (#F59E0B), white text, full-width, rounded-xl
- Hover: bg-amber/90
- Disabled during loading: opacity-50, cursor-not-allowed
- Loading state: text "Sending..."

**Success state:**
- Centered check icon (amber background circle)
- Heading: "Message Sent"
- Body: "We will get back to you within 24 hours."
- Close button: Navy background, white text

**Error state:**
- Red text (#EF4444), 14px
- Below relevant field
- Form stays open, fields retain values

**Body scroll lock:** When modal open, document.body.style.overflow = 'hidden'; cleanup on close/unmount
</action>
  <verify>
    <automated>grep -l "pulse-ring" app/globals.css && grep -l "FloatingContactButton" app/components/FloatingContactButton.tsx && grep -l "animate-pulse-ring" app/components/FloatingContactButton.tsx</automated>
  </verify>
  <done>Floating button renders with pulse animation, modal opens on click with form, ESC closes modal</done>
</task>

</tasks>

<verification>
- Component file created with all states (idle, hover, modal open, submitting, success, error)
- API endpoint validates email + message, sends email via nodemailer
- Pulse ring animation defined in globals.css
- No console errors in build
</verification>

<success_criteria>
- `/api/contact` POST returns 200 with {ok: true} for valid input
- FloatingContactButton.tsx exports default function
- Button positioned bottom-right 20px from edges
- Pulse ring animates continuously (disabled for prefers-reduced-motion)
- Modal opens on click, closes on ESC/overlay click
- Form submits to /api/contact, shows success state on 200
</success_criteria>

<output>
After completion, create `.planning/phases/16-floating-contact-button/16-01-SUMMARY.md`
</output>
