---
phase: 15
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/DirectorySection/types.ts
  - app/components/DirectorySection/data/directory-cities.ts
  - app/components/DirectorySection/FilterTabs.tsx
  - app/components/DirectorySection/CityList.tsx
  - app/components/DirectorySection/DirectoryMap.tsx
  - app/components/DirectorySection/index.tsx
autonomous: true
requirements:
  - SEO-01

must_haves:
  truths:
    - "User can see factory directory section with city list and interactive map"
    - "User can filter directory by 8 industry categories"
    - "User can click city in list and see map pan to that location"
    - "User can click map marker and see city highlighted in list"
    - "User sees amber markers representing factory locations"
    - "User sees marker clusters when zoomed out"
  artifacts:
    - path: "app/components/DirectorySection/types.ts"
      provides: "CityEntry interface defining city data structure"
      exports: "CityEntry, IndustryFilter"
    - path: "app/components/DirectorySection/data/directory-cities.ts"
      provides: "10 hardcoded cities with coordinates and industry tags"
      exports: "directoryCities"
    - path: "app/components/DirectorySection/FilterTabs.tsx"
      provides: "8 industry filter tabs with amber active state"
      exports: "default export"
    - path: "app/components/DirectorySection/CityList.tsx"
      provides: "Scrollable city list with IntersectionObserver animation"
      exports: "default export"
    - path: "app/components/DirectorySection/DirectoryMap.tsx"
      provides: "Leaflet map with marker clustering and dynamic import"
      exports: "default export"
    - path: "app/components/DirectorySection/index.tsx"
      provides: "Main wrapper with 70/30 layout, state management, CTA"
      exports: "default export"
  key_links:
    - from: "app/components/DirectorySection/index.tsx"
      to: "app/components/DirectorySection/FilterTabs.tsx"
      via: "activeFilter state"
    - from: "app/components/DirectorySection/index.tsx"
      to: "app/components/DirectorySection/CityList.tsx"
      via: "filteredCities prop, selectedCity prop, onCitySelect callback"
    - from: "app/components/DirectorySection/index.tsx"
      to: "app/components/DirectorySection/DirectoryMap.tsx"
      via: "filteredCities prop, selectedCity prop, onCitySelect callback"
    - from: "CityList.tsx onCitySelect"
      to: "DirectoryMap.tsx"
      via: "selectedCity prop update triggers map pan"
    - from: "DirectoryMap.tsx marker click"
      to: "CityList.tsx"
      via: "onCitySelect callback triggers list scroll"
---

<objective>
Create the DirectorySection component - a factory directory landing page featuring a city list (30%) synchronized with an interactive Leaflet map (70%), with industry filter tabs. Fully replaces the existing Industries section on homepage.
</objective>

<execution_context>
@/Users/mark/Projects/wag/app/components/TwoWaysAccess.tsx
@/Users/mark/Projects/wag/app/components/industries/index.tsx
@/Users/mark/Projects/wag/.planning/phases/15-directory-section-landing-page/15-CONTEXT.md
@/Users/mark/Projects/wag/.planning/phases/15-directory-section-landing-page/15-UI-SPEC.md
</execution_context>

<interfaces>
<!-- CityEntry interface from 15-CONTEXT.md decisions -->
interface CityEntry {
  city: string        // "Foshan"
  province: string    // "Guangdong"
  factories: number   // 80
  focus: string       // "Furniture manufacturing hub"
  coords: [number, number]  // [lat, lng]
  industries: string[] // ["Furniture", "Construction"]
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript types and hardcoded city data</name>
  <files>
    - app/components/DirectorySection/types.ts
    - app/components/DirectorySection/data/directory-cities.ts
  </files>
  <read_first>
    - app/components/DirectorySection/types.ts (new file - interface contract)
    - app/components/DirectorySection/data/directory-cities.ts (new file - data source)
    - app/page.tsx (to understand current imports)
  </read_first>
  <action>
Create two files:

**1. app/components/DirectorySection/types.ts:**
```typescript
export interface CityEntry {
  city: string
  province: string
  factories: number
  focus: string
  coords: [number, number]
  industries: string[]
}

export type IndustryFilter = 'All' | 'Electronics' | 'Furniture' | 'Robotics' | 'EV Battery' | 'CBD Property' | 'Construction' | 'Food & Beverage'
```

**2. app/components/DirectorySection/data/directory-cities.ts:**
Hardcode CityEntry[] array with these cities (use coords from research):
- Foshan, Guangdong, 80 factories, coords [23.0218, 113.1219], industries: Furniture, Construction
- Dongguan, Guangdong, 50 factories, coords [23.0209, 113.7518], industries: Electronics, Robotics
- Hangzhou, Zhejiang, 35 factories, coords [30.2741, 120.1551], industries: Electronics, Robotics
- Guangzhou, Guangdong, 60 factories, coords [23.1291, 113.2644], industries: Electronics, Furniture
- Shenzhen, Guangdong, 75 factories, coords [22.5431, 114.0579], industries: Electronics, Robotics
- Suzhou, Jiangsu, 40 factories, coords [31.2989, 120.5853], industries: Electronics, Robotics
- Shanghai, Shanghai, 55 factories, coords [31.2304, 121.4737], industries: Electronics, Furniture
- Chengdu, Sichuan, 25 factories, coords [30.5728, 104.0668], industries: Food & Beverage, Construction
- Beijing, Beijing, 30 factories, coords [39.9042, 116.4074], industries: Electronics, CBD Property
- Wuhan, Hubei, 20 factories, coords [30.5928, 114.3055], industries: Construction, Food & Beverage

Export as: `export const directoryCities: CityEntry[] = [...]`
</action>
  <verify>
    <automated>grep -l "export interface CityEntry" app/components/DirectorySection/types.ts && grep -l "export const directoryCities" app/components/DirectorySection/data/directory-cities.ts</automated>
  </verify>
  <done>types.ts defines CityEntry interface, directory-cities.ts exports hardcoded city array with 10 cities</done>
</task>

<task type="auto">
  <name>Task 2: Create FilterTabs component</name>
  <files>app/components/DirectorySection/FilterTabs.tsx</files>
  <read_first>
    - app/components/DirectorySection/types.ts (imports IndustryFilter)
    - app/components/TwoWaysAccess.tsx (animation patterns, CTA button styles)
  </read_first>
  <action>
Create FilterTabs.tsx component:

**Requirements:**
- Props: `activeFilter: IndustryFilter`, `onFilterChange: (filter: IndustryFilter) => void`
- Horizontal scrollable tabs: All, Electronics, Furniture, Robotics, EV Battery, CBD Property, Construction, Food & Beverage
- Tab styles: `text-xs font-semibold uppercase tracking-[0.08em]` default, `text-amber` when active
- Active tab: amber underline (2px solid amber) + amber text
- Hover: amber/10 background
- Animation: underline slides to active tab (200ms ease-in-out)
- Container: flex gap, overflow-x-auto, hide scrollbar

**Design tokens from TwoWaysAccess.tsx:**
- CTA button: `min-h-11 px-6 py-3 text-sm font-semibold`
- Amber: `#F59E0B`
- Navy: `#0F2D5E`
</action>
  <verify>
    <automated>grep -l "IndustryFilter" app/components/DirectorySection/FilterTabs.tsx && grep -c "onFilterChange" app/components/DirectorySection/FilterTabs.tsx</automated>
  </verify>
  <done>FilterTabs renders 8 industry filter tabs, accepts activeFilter and onFilterChange props, amber underline on active tab</done>
</task>

<task type="auto">
  <name>Task 3: Create CityList component with IntersectionObserver animation</name>
  <files>app/components/DirectorySection/CityList.tsx</files>
  <read_first>
    - app/components/DirectorySection/types.ts (CityEntry interface)
    - app/components/DirectorySection/data/directory-cities.ts (imports directoryCities)
    - app/components/TwoWaysAccess.tsx (IntersectionObserver pattern lines 43-58, stagger animation)
  </read_first>
  <action>
Create CityList.tsx component:

**Requirements:**
- Props: `cities: CityEntry[]`, `selectedCity: string | null`, `onCitySelect: (city: string) => void`
- Scrollable list panel (30% width on desktop, full on mobile)
- Each city item displays:
  - City name (bold, 18px, navy)
  - Province (inline, muted/navy/60)
  - "{N} factories" (factory count)
  - Focus keyword (e.g., "Furniture manufacturing hub")
- Item states:
  - Default: white background
  - Hover: amber/10 background (transition 200ms)
  - Selected: amber border-left (3px)
- Animation: IntersectionObserver stagger fade-in, delay: index * 80ms
  - Initial: `opacity-0 translate-y-4`
  - Visible: `opacity-100 translate-y-0`, duration 700ms, ease-out
- Reduced motion: respect `prefers-reduced-motion` (instant fade)

**Section styling from UI-SPEC:**
- Container: `bg-[#f0f4f8]` (matching existing section background)
- Item padding: `p-4`
- Card style: `rounded-lg`, `border`, `shadow-sm`
</action>
  <verify>
    <automated>grep -l "IntersectionObserver" app/components/DirectorySection/CityList.tsx && grep -c "opacity-0 translate-y-4" app/components/DirectorySection/CityList.tsx</automated>
  </verify>
  <done>CityList renders scrollable city items with IntersectionObserver stagger animation, hover/selected states</done>
</task>

<task type="auto">
  <name>Task 4: Create DirectoryMap component with Leaflet + clustering</name>
  <files>app/components/DirectorySection/DirectoryMap.tsx</files>
  <read_first>
    - app/components/DirectorySection/types.ts (CityEntry interface)
    - app/components/DirectorySection/data/directory-cities.ts (imports directoryCities)
    - app/components/TwoWaysAccess.tsx (reduced motion handling lines 177-189)
  </read_first>
  <action>
Create DirectoryMap.tsx component:

**IMPORTANT: This file must use 'use client' and be loaded via dynamic import with ssr: false**

**Requirements:**
- Props: `cities: CityEntry[]`, `selectedCity: string | null`, `onCitySelect: (city: string) => void`
- Import Leaflet CSS: `import 'leaflet/dist/leaflet.css'`
- Import markercluster CSS: `import 'leaflet.markercluster/dist/MarkerCluster.css'` and `import 'leaflet.markercluster/dist/MarkerCluster.Default.css'`

**Map setup:**
- Initialize map with center [35.0, 105.0] (central China), zoom 5
- Use OpenStreetMap tiles: `https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png`
- Custom amber markers: create custom icon using DivIcon with amber circle
- Clustering: use MarkerClusterGroup with options: maxClusterRadius: 50, spiderfyOnMaxZoom: true, showCoverageOnHover: false

**Popup content (custom styled):**
- City name: bold, navy header
- Factory count: "{N} verified factories"
- CTA button: "View Directory" (amber button)
- Popup styling: match design system (navy header, white body, amber CTA)

**Interaction:**
- Click on marker: calls `onCitySelect(city)` to sync with list
- `selectedCity` prop: if set, pan/zoom to that city and open popup

**SSR handling:** Export default wrapped in dynamic:
```typescript
const DirectoryMap = dynamic(() => import('./DirectoryMapInner'), { ssr: false })
```
But implement as single file with 'use client' at top and dynamic import handled by parent.

Actually - create DirectoryMapInner.tsx as the actual map component, and DirectoryMap.tsx as the dynamic wrapper.

**DirectoryMap.tsx (wrapper):**
```typescript
'use client'
import dynamic from 'next/dynamic'
const DirectoryMapInner = dynamic(() => import('./DirectoryMapInner'), { ssr: false })
export default function DirectoryMap(props) {
  return <DirectoryMapInner {...props} />
}
```

**DirectoryMapInner.tsx:**
- 'use client' directive
- All Leaflet logic
- useEffect for map initialization
- useEffect for updating markers when cities prop changes
- useEffect for panning to selectedCity
</action>
  <verify>
    <automated>grep -l "use client" app/components/DirectorySection/DirectoryMap.tsx && grep -l "MarkerClusterGroup" app/components/DirectorySection/DirectoryMapInner.tsx && grep -l "dynamic.*ssr: false" app/components/DirectorySection/DirectoryMap.tsx</automated>
  </verify>
  <done>DirectoryMap renders Leaflet map with amber custom markers, marker clustering, custom popups, list-map synchronization via props</done>
</task>

<task type="auto">
  <name>Task 5: Create DirectorySection wrapper component</name>
  <files>app/components/DirectorySection/index.tsx</files>
  <read_first>
    - app/components/DirectorySection/types.ts
    - app/components/DirectorySection/FilterTabs.tsx
    - app/components/DirectorySection/CityList.tsx
    - app/components/DirectorySection/DirectoryMap.tsx
    - app/components/DirectorySection/data/directory-cities.ts
    - app/components/TwoWaysAccess.tsx (section wrapper patterns)
  </read_first>
  <action>
Create DirectorySection/index.tsx - the main wrapper component:

**State:**
- `activeFilter: IndustryFilter` (default: 'All')
- `selectedCity: string | null`
- `visible: boolean` (IntersectionObserver for section entrance)

**Derived:**
- `filteredCities`: filter `directoryCities` by activeFilter (if 'All', show all)

**Layout:**
- Section wrapper: `bg-white py-14 md:py-18 px-4 md:px-10` + `max-w-[1100px] mx-auto`
- Section header:
  - Label: "Factory Directory" (uppercase amber label, `text-xs font-semibold uppercase tracking-[0.12em]`)
  - Heading: "Explore Verified Manufacturers" (font-serif, navy, `text-[clamp(32px,5vw,48px)]`)

**Desktop (>= 768px):** CSS grid `grid-cols-[30%_1fr]` (list 30%, map 70%)
- Left: CityList with filteredCities
- Right: DirectoryMap with filteredCities

**Mobile (< 768px):**
- Map on top (h-[300px] fixed height)
- CityList below (scrollable)

**FilterTabs:** Above the grid, between header and content

**Interaction wiring:**
- FilterTabs onFilterChange -> updates activeFilter
- CityList onCitySelect -> updates selectedCity
- DirectoryMap selectedCity prop -> receives selectedCity for syncing

**Bottom CTA:**
- "View Full Directory" button with arrow SVG
- Links to /enquiry
- Style: `min-h-11 px-6 py-3 text-sm font-semibold bg-amber text-white hover:bg-amber/90`

**Animation:**
- Section entrance: IntersectionObserver (threshold 0.15), fade-in + translateY
- Reduced motion: respect prefers-reduced-motion

**Empty state:**
- When filteredCities.length === 0:
  - Heading: "No factories found"
  - Body: "Try selecting a different industry filter above."
</action>
  <verify>
    <automated>grep -l "activeFilter" app/components/DirectorySection/index.tsx && grep -l "filteredCities" app/components/DirectorySection/index.tsx && grep -l "View Full Directory" app/components/DirectorySection/index.tsx</automated>
  </verify>
  <done>DirectorySection renders complete layout with header, filter tabs, 70/30 map-list grid, CTA, empty state handling</done>
</task>

</tasks>

<verification>
- All 6 files created in app/components/DirectorySection/
- types.ts exports CityEntry interface and IndustryFilter type
- directory-cities.ts exports 10 cities with coords
- FilterTabs renders 8 industry tabs with amber active state
- CityList has IntersectionObserver stagger animation
- DirectoryMapInner uses Leaflet with marker clustering
- DirectoryMap.tsx is dynamic import wrapper with ssr: false
- DirectorySection/index.tsx wires all components with 70/30 layout
- Section header matches UI-SPEC copywriting contract
</verification>

<success_criteria>
DirectorySection component:
- [ ] Renders 8 industry filter tabs (All, Electronics, Furniture, Robotics, EV Battery, CBD Property, Construction, Food & Beverage)
- [ ] Displays city list on left (30% width) with scrollable items
- [ ] Renders Leaflet map on right (70% width) with amber markers
- [ ] Markers cluster when zoomed out
- [ ] Clicking city in list pans map to that city
- [ ] Clicking map marker updates selected city in list
- [ ] Filter tabs update both map markers and list items
- [ ] Bottom CTA links to /enquiry
- [ ] Mobile: map top (300px), list below
- [ ] IntersectionObserver animations on list items
- [ ] Empty state when no matches
</success_criteria>

<output>
After completion, create `.planning/phases/15-directory-section-landing-page/15-01-SUMMARY.md`
</output>
