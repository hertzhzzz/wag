---
phase: 06.1-vercel-gmailapp-password-user-enquiry
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "GMAIL_USER is configured in Vercel production environment"
    - "GMAIL_APP_PASSWORD is configured in Vercel production environment"
    - "Production deployment has access to Gmail credentials"
    - "Enquiry form can successfully send emails in production"
  artifacts:
    - path: "Vercel Production Environment"
      provides: "Environment variables for email service"
      contains: "GMAIL_USER, GMAIL_APP_PASSWORD"
  key_links:
    - from: "app/api/enquiry/route.ts"
      to: "Vercel Environment"
      via: "process.env.GMAIL_USER, process.env.GMAIL_APP_PASSWORD"
      pattern: "getTransporter.*GMAIL_USER.*GMAIL_APP_PASSWORD"
---

<objective>
Configure Gmail credentials in Vercel production environment to fix the enquiry form email functionality.

Purpose: Sync GMAIL_USER and GMAIL_APP_PASSWORD from local .env.local to Vercel so the /api/enquiry endpoint can send emails in production.

Output: GMAIL_USER and GMAIL_APP_PASSWORD configured in Vercel, enquiry form working on production.
</objective>

<context>
@.planning/phases/06.1-vercel-gmailapp-password-user-enquiry/06.1-CONTEXT.md
@.planning/phases/06.1-vercel-gmailapp-password-user-enquiry/06.1-RESEARCH.md
@/Users/mark/Projects/wag/app/api/enquiry/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GMAIL_USER to Vercel production environment</name>
  <files>.env.local</files>
  <read_first>
    - .env.local (to read current GMAIL_USER value)
  </read_first>
  <action>
    Add GMAIL_USER environment variable to Vercel production using CLI:

    Run: vercel env add GMAIL_USER production

    When prompted, enter value: mark@winningadventure.com.au

    Alternative via Dashboard:
    1. Go to Vercel Dashboard → wag project → Settings → Environment Variables
    2. Add: Name=GMAIL_USER, Value=mark@winningadventure.com.au, Environment=Production
    3. Click Save
  </action>
  <verify>
    <automated>vercel env ls production | grep GMAIL_USER</automated>
  </verify>
  <acceptance_criteria>
    - Output contains "GMAIL_USER"
    - Environment variable is visible in vercel env ls production
  </acceptance_criteria>
  <done>GMAIL_USER configured in Vercel production environment</done>
</task>

<task type="auto">
  <name>Task 2: Add GMAIL_APP_PASSWORD to Vercel production environment</name>
  <files>.env.local</files>
  <read_first>
    - .env.local (to read current GMAIL_APP_PASSWORD value)
  </read_first>
  <action>
    Add GMAIL_APP_PASSWORD environment variable to Vercel production using CLI (as sensitive):

    Run: vercel env add GMAIL_APP_PASSWORD production --sensitive

    When prompted, enter value: wyxa ktwu odib hqbu

    Alternative via Dashboard:
    1. Go to Vercel Dashboard → wag project → Settings → Environment Variables
    2. Add: Name=GMAIL_APP_PASSWORD, Value=wyxa ktwu odib hqbu, Environment=Production, check "Sensitive"
    3. Click Save
  </action>
  <verify>
    <automated>vercel env ls production | grep GMAIL_APP_PASSWORD</automated>
  </verify>
  <acceptance_criteria>
    - Output contains "GMAIL_APP_PASSWORD"
    - Environment variable is marked as sensitive in Vercel
  </acceptance_criteria>
  <done>GMAIL_APP_PASSWORD configured in Vercel production environment (sensitive)</done>
</task>

<task type="auto">
  <name>Task 3: Trigger production redeploy</name>
  <files></files>
  <read_first>
  </read_first>
  <action>
    Trigger a new deployment so environment variables take effect. Options:

    Option A - Push a minor commit:
    ```bash
    git add .
    git commit -m "chore: trigger redeploy for Gmail env vars"
    git push origin master
    ```

    Option B - Use Vercel CLI:
    ```bash
    vercel --prod
    ```

    Option C - Use Vercel Dashboard:
    1. Go to Vercel Dashboard → Deployments
    2. Click "..." on latest deployment → "Redeploy"
  </action>
  <verify>
    <automated>vercel ls 2>/dev/null | head -5</automated>
  </verify>
  <acceptance_criteria>
    - New deployment shows in vercel ls output
    - Deployment status is "Ready" or "Building"
  </acceptance_criteria>
  <done>Production redeploy triggered with new environment variables</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Verify enquiry form sends email</name>
  <what-built>Enquiry form with Gmail credentials configured</what-built>
  <how-to-verify>
    1. Wait for deployment to complete (check Vercel Dashboard)
    2. Visit https://wag-three.vercel.app/enquiry (or your production URL)
    3. Fill out the enquiry form with test data:
       - Full Name: Test User
       - Company Name: Test Company
       - Email: test@example.com
       - Phone: (optional)
       - Industry: Manufacturing
       - Looking For: Testing email functionality
    4. Click Submit
    5. Verify success message appears
    6. Check inbox for mark@winningadventure.com.au - should receive email with enquiry details
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the form submission</resume-signal>
  <done>Enquiry form successfully sends email to GMAIL_USER address</done>
</task>

</tasks>

<verification>
1. Run `vercel env ls production` to confirm both GMAIL_USER and GMAIL_APP_PASSWORD are listed
2. Check deployment status is "Ready" in Vercel Dashboard
3. Submit test enquiry form and verify email received
</verification>

<success_criteria>
- [ ] GMAIL_USER=mark@winningadventure.com.au configured in Vercel Production
- [ ] GMAIL_APP_PASSWORD configured in Vercel Production (sensitive)
- [ ] Production deployment completed with new environment variables
- [ ] /enquiry form submission sends email successfully
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-vercel-gmailapp-password-user-enquiry/06.1-01-SUMMARY.md`
</output>
