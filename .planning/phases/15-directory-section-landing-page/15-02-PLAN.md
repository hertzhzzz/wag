---
phase: 15
plan: '02'
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - app/page.tsx
  - package.json
autonomous: true
requirements:
  - SEO-01

must_haves:
  truths:
    - "Homepage renders DirectorySection instead of Industries section"
    - "Leaflet dependencies are installed and importable"
  artifacts:
    - path: "package.json"
      provides: "Leaflet dependencies installed"
      exports: "leaflet, react-leaflet, leaflet.markercluster"
    - path: "app/page.tsx"
      provides: "DirectorySection imported and rendered"
      exports: "default export with DirectorySection"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/DirectorySection/index.tsx"
      via: "import DirectorySection"
    - from: "app/page.tsx"
      to: "app/components/industries/index.tsx"
      via: "removed import"
---

<objective>
Replace the Industries section on the homepage with the new DirectorySection component, and install required Leaflet dependencies.
</objective>

<execution_context>
@/Users/mark/Projects/wag/app/page.tsx
@/Users/mark/Projects/wag/app/components/DirectorySection/index.tsx
@/Users/mark/Projects/wag/.planning/phases/15-directory-section-landing-page/15-01-SUMMARY.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Install Leaflet dependencies</name>
  <files>package.json</files>
  <read_first>
    - package.json (current dependencies)
  </read_first>
  <action>
Run npm install to add Leaflet packages:

```bash
npm install leaflet react-leaflet leaflet.markercluster
npm install --save-dev @types/leaflet @types/leaflet.markercluster
```

These packages enable:
- leaflet: Core Leaflet library
- react-leaflet: React components for Leaflet
- leaflet.markercluster: Marker clustering plugin
- @types/*: TypeScript type definitions
</action>
  <verify>
    <automated>grep -E "leaflet|react-leaflet|leaflet.markercluster" package.json</automated>
  </verify>
  <done>Leaflet packages installed (leaflet, react-leaflet, leaflet.markercluster, @types/*)</done>
</task>

<task type="auto">
  <name>Task 2: Replace Industries with DirectorySection in homepage</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current state showing Industries import)
    - app/components/DirectorySection/index.tsx (new component to import)
  </read_first>
  <action>
**In app/page.tsx:**

1. Change import from:
```typescript
import Industries from './components/industries'
```

To:
```typescript
import DirectorySection from './components/DirectorySection'
```

2. Change component usage from:
```typescript
<Industries />
```

To:
```typescript
<DirectorySection />
```

The DirectorySection replaces the Industries section in the homepage layout (between TwoWaysAccess and FAQ).
</action>
  <verify>
    <automated>grep -l "import DirectorySection" app/page.tsx && grep -c "<DirectorySection />" app/page.tsx</automated>
  </verify>
  <done>Homepage imports and renders DirectorySection instead of Industries</done>
</task>

</tasks>

<verification>
- package.json contains leaflet dependencies
- app/page.tsx imports DirectorySection from components/DirectorySection
- app/page.tsx renders DirectorySection in the page component tree
- npm run build passes
</verification>

<success_criteria>
- [ ] Leaflet packages installed (leaflet, react-leaflet, leaflet.markercluster, @types/*)
- [ ] Homepage renders DirectorySection component
- [ ] Industries section no longer imported in page.tsx
- [ ] npm run build passes without errors
- [ ] 5 key pages still accessible: /, /services, /about, /resources, /enquiry
</success_criteria>

<output>
After completion, create `.planning/phases/15-directory-section-landing-page/15-02-SUMMARY.md`
</output>
