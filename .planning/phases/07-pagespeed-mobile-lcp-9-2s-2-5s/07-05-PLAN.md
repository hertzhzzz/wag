---
phase: 07-pagespeed-mobile-lcp-9-2s-2-5s
plan: 05
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - next.config.js
  - package.json
autonomous: true
requirements:
  - LCP-03
must_haves:
  truths:
    - "Bundle analyzer installed and configured"
    - "ANALYZE=true npm run build generates bundle breakdown"
  artifacts:
    - path: "package.json"
      provides: "Bundle analyzer dependency"
      contains: "@next/bundle-analyzer"
    - path: "next.config.js"
      provides: "Bundle analyzer config"
      contains: "bundle-analyzer"
---

<objective>
Analyze JavaScript bundle to identify unused code and opportunities for optimization.
</objective>

<context>
@.planning/phases/07-pagespeed-mobile-lcp-9-2s-2-5s/07-CONTEXT.md
@/Users/mark/Projects/wag/next.config.js

**Purpose:** Bundle analysis helps identify large JavaScript modules that can be optimized through dynamic imports or code splitting.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install bundle analyzer</name>
  <files>package.json</files>
  <action>
Install @next/bundle-analyzer as a dev dependency:
```bash
npm install --save-dev @next/bundle-analyzer
```
  </action>
  <acceptance_criteria>
- @next/bundle-analyzer is added to package.json devDependencies
  </acceptance_criteria>
  <verify>
grep -n '@next/bundle-analyzer' package.json
  </verify>
  <done>Bundle analyzer installed</done>
</task>

<task type="auto">
  <name>Task 2: Configure bundle analyzer in next.config.js</name>
  <read_first>/Users/mark/Projects/wag/next.config.js</read_first>
  <files>next.config.js</files>
  <action>
Add bundle analyzer configuration to next.config.js. Wrap the existing config with the bundle analyzer:
```javascript
const withBundleAnalyzer = require('@next/bundle-analyzer')({
  enabled: process.env.ANALYZE === 'true',
})

module.exports = withBundleAnalyzer({
  // existing config
})
```
  </action>
  <acceptance_criteria>
- next.config.js contains bundle analyzer configuration
- Config is conditional (enabled only when ANALYZE=true)
  </acceptance_criteria>
  <verify>
grep -n 'bundle-analyzer' next.config.js
  </verify>
  <done>Bundle analyzer configured</done>
</task>

<task type="auto">
  <name>Task 3: Run bundle analysis</name>
  <files>package.json</files>
  <action>
Run bundle analysis to identify large modules:
```bash
ANALYZE=true npm run build
```
Review the output to identify optimization opportunities.
  </action>
  <acceptance_criteria>
- Bundle analysis completes and shows breakdown by module
- Output can be used to identify large dependencies
  </acceptance_criteria>
  <verify>
ANALYZE=true npm run build 2>&1 | grep -E "(bundle|module|size)" | head -20
  </verify>
  <done>Bundle analysis completed</done>
</task>

</tasks>

<verification>
- Bundle analyzer is configured and functional
- Analysis output is generated when running ANALYZE=true npm run build
</verification>

<success_criteria>
Bundle analysis complete - can identify large JS modules for optimization
</success_criteria>

<output>
After completion, create `.planning/phases/07-pagespeed-mobile-lcp-9-2s-2-5s/07-05-SUMMARY.md`
</output>
