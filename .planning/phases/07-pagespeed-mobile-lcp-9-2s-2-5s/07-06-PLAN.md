---
phase: 07-pagespeed-mobile-lcp-9-2s-2-5s
plan: 06
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - public/
autonomous: true
requirements:
  - LCP-03
must_haves:
  truths:
    - "SVG files inventoried and assessed"
    - "Large SVGs identified or confirmed as already optimized"
  artifacts:
    - path: "public/"
      provides: "SVG inventory"
---

<objective>
Optimize SVG assets by reducing file size through removing metadata and unused elements.
</objective>

<context>
@/Users/mark/Projects/wag/public/

**Purpose:** SVGs often contain metadata, editor comments, and unused definitions that can be removed to reduce payload size.
</context>

<tasks>

<task type="auto">
<name>Task 1: Identify large SVGs</name>
<files>public/</files>
<action>
List all SVG files in public/ directory and identify any that are larger than 5KB:
```bash
ls -la public/**/*.svg 2>/dev/null | awk '{if($5>5000) print $0}'
```
Or find all:
```bash
find public -name "*.svg" -exec ls -lh {} \;
```
  </action>
  <acceptance_criteria>
- All SVG files in public/ are identified
- List of large SVGs (>5KB) is available
  </acceptance_criteria>
  <verify>
find public -name "*.svg" -exec ls -lh {} \; | grep -v "0 "
  </verify>
  <done>SVG inventory complete</done>
</task>

<task type="auto">
<name>Task 2: Analyze SVG optimization opportunities</name>
<files>public/</files>
<action>
For any large SVGs identified, check if they contain removable content:
- Metadata elements (inkscape:, sodipodi:, etc.)
- Editor comments
- Unused definitions
- Excessive decimal precision

Note: If all SVGs are small (<5KB), skip manual optimization.
  </action>
  <acceptance_criteria>
- SVGs are assessed for optimization potential
- Decision made whether to optimize or skip
  </acceptance_criteria>
  <verify>
[Check completed - either SVGs optimized or assessed as already small]
  </verify>
  <done>SVG analysis complete</done>
</task>

</tasks>

<verification>
- SVG inventory completed
- Large SVGs identified and assessed
</verification>

<success_criteria>
SVG optimization complete - SVGs are either optimized or confirmed as already small
</success_criteria>

<output>
After completion, create `.planning/phases/07-pagespeed-mobile-lcp-9-2s-2-5s/07-06-SUMMARY.md`
</output>
