---
phase: 07-pagespeed-mobile-lcp-9-2s-2-5s
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Hero.tsx
autonomous: true
requirements:
  - LCP-01
  - LCP-02
must_haves:
  truths:
    - "Video preload set to 'none' to prevent any video loading"
    - "Hero image has fetchpriority='high' for explicit priority hint"
    - "Build passes without errors"
  artifacts:
    - path: "app/components/Hero.tsx"
      provides: "Video preload fix + fetchpriority"
      contains: "preload=\"none\""
      contains2: "fetchPriority=\"high\""
---

<objective>
Fix critical video and image loading issues identified in research. Change video preload from "metadata" to "none" and add fetchpriority="high" to hero image for maximum LCP optimization.
</objective>

<context>
@.planning/phases/07-pagespeed-mobile-lcp-9-2s-2-5s/07-RESEARCH.md
@.planning/phases/07-pagespeed-mobile-lcp-9-2s-2-5s/07-CONTEXT.md
@/Users/mark/Projects/wag/app/components/Hero.tsx

**Critical findings from research:**
1. Video uses `preload="metadata"` which loads video even when CSS-hidden (md:hidden) - MUST change to `preload="none"`
2. Missing `fetchpriority="high"` on hero image - must add alongside `priority={true}`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change video preload from metadata to none</name>
  <read_first>/Users/mark/Projects/wag/app/components/Hero.tsx</read_first>
  <files>app/components/Hero.tsx</files>
  <action>
Change line 29 from `preload="metadata"` to `preload="none"`. This prevents the browser from loading any video data until playback begins, which is critical for mobile LCP since CSS hiding (md:hidden) does NOT prevent the browser from processing the video element and loading its metadata.
  </action>
  <acceptance_criteria>
- grep -n 'preload="none"' app/components/Hero.tsx returns the line with preload="none"
- grep -n 'preload="metadata"' app/components/Hero.tsx returns no results
  </acceptance_criteria>
  <verify>
grep -n 'preload="none"' app/components/Hero.tsx
  </verify>
  <done>Video preload changed from "metadata" to "none" to prevent video loading on all viewports</done>
</task>

<task type="auto">
  <name>Task 2: Add fetchpriority="high" to mobile hero image</name>
  <read_first>/Users/mark/Projects/wag/app/components/Hero.tsx</read_first>
  <files>app/components/Hero.tsx</files>
  <action>
Add fetchpriority="high" attribute to the mobile hero Image component (line 9-16). This provides explicit priority hint to browsers in addition to Next.js's priority prop. Add the attribute: fetchPriority="high" alongside the existing priority={true}.
  </action>
  <acceptance_criteria>
- grep -n 'fetchPriority="high"' app/components/Hero.tsx returns the line with fetchPriority
- The mobile Image component has both priority={true} and fetchPriority="high"
  </acceptance_criteria>
  <verify>
grep -n 'fetchPriority="high"' app/components/Hero.tsx
  </verify>
  <done>Hero image has explicit fetchpriority="high" for maximum LCP optimization</done>
</task>

<task type="auto">
  <name>Task 3: Verify build passes</name>
  <files>app/components/Hero.tsx</files>
  <action>
Run npm run build to verify no errors introduced by the changes.
  </action>
  <acceptance_criteria>
- npm run build completes without errors
- Build output shows no TypeScript or lint errors
  </acceptance_criteria>
  <verify>
npm run build 2>&1 | tail -20
  </verify>
  <done>Build passes successfully</done>
</task>

</tasks>

<verification>
- Verify preload="none" is present in Hero.tsx
- Verify fetchpriority="high" is present in Hero.tsx
- Verify build completes without errors
</verification>

<success_criteria>
Video preload changed to "none" and hero image has fetchpriority="high" - both critical for achieving mobile LCP < 2.5s
</success_criteria>

<output>
After completion, create `.planning/phases/07-pagespeed-mobile-lcp-9-2s-2-5s/07-02-SUMMARY.md`
</output>
