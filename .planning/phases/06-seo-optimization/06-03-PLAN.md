---
phase: 06-seo-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/services/page.tsx
  - app/about/page.tsx
autonomous: true
requirements:
  - SEO-04
  - SEO-05
must_haves:
  truths:
    - "LocalBusiness schema includes Adelaide address for local SEO"
    - "Hero images have priority loading for LCP optimization"
    - "All images use next/image with proper dimensions"
    - "Local keywords present in page metadata"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Enhanced LocalBusiness schema with Adelaide address"
      contains: "LocalBusiness"
    - path: "app/page.tsx"
      provides: "Priority-loaded hero image for LCP"
      contains: "priority={true}"
    - path: "app/services/page.tsx"
      provides: "next/image with dimensions"
      contains: "next/image"
    - path: "app/about/page.tsx"
      provides: "Local SEO keywords in metadata"
      contains: "adelaide"
  key_links:
    - from: "app/page.tsx"
      to: "hero image"
      via: "priority prop"
      pattern: "priority"
---

<objective>
Optimize Core Web Vitals and local SEO for Adelaide market

Purpose: Core Web Vitals affect rankings; Local SEO targets Adelaide-based searches
Output: Enhanced LocalBusiness schema, optimized images for LCP, local keywords in metadata
</objective>

<context>
@/Users/mark/Projects/wag/.planning/phases/06-seo-optimization/06-CONTEXT.md
@/Users/mark/Projects/wag/.planning/phases/06-seo-optimization/06-RESEARCH.md
@/Users/mark/Projects/wag/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance LocalBusiness schema in layout.tsx for Adelaide</name>
  <files>app/layout.tsx</files>
  <read_first>app/layout.tsx (read current JSON-LD structure)</read_first>
  <action>
Enhance existing LocalBusiness JSON-LD in app/layout.tsx. Add the following properties to the existing schema:

For LocalBusiness (Adelaide focus):
- @type: "LocalBusiness" (keep existing Organization)
- address: { @type: "PostalAddress", streetAddress: "Adelaide, South Australia", addressLocality: "Adelaide", addressRegion: "SA", addressCountry: "AU", postalCode: "5000" }
- areaServed: { @type: "State", name: "South Australia" }, { @type: "Country", name: "Australia" }
- priceRange: "AUD $2,000 - $50,000+"
- serviceArea: { @type: "Country", name: "Australia" }

Keep existing Organization schema alongside LocalBusiness (Google allows multiple entities).
  </action>
  <acceptance_criteria>
- app/layout.tsx contains enhanced LocalBusiness schema
- Address includes Adelaide, SA, Australia
- areaServed includes Australia
- Schema remains valid JSON-LD (no syntax errors)
  </acceptance_criteria>
  <verify>
grep -n "LocalBusiness" app/layout.tsx
grep -n "Adelaide" app/layout.tsx
  </verify>
  <done>LocalBusiness schema enhanced for Adelaide with Australian address</done>
</task>

<task type="auto">
  <name>Task 2: Optimize hero images with priority loading for LCP</name>
  <files>app/page.tsx</files>
  <read_first>app/page.tsx (read current image usage)</read_first>
  <action>
Add priority prop to hero images above the fold in app/page.tsx to improve LCP score:

1. Find hero/banner images in page.tsx that appear above the fold
2. Add priority={true} prop to those next/image components
3. Ensure alt text is descriptive (not generic like "hero image")

For example:
```tsx
<Image
  src="/hero-image.jpg"
  alt="Australian business team at Chinese factory"
  width={1200}
  height={600}
  priority={true}  // Add this for LCP optimization
  className="..."
/>
```

If hero is a background image in CSS, ensure proper sizing/loading approach.
  </action>
  <acceptance_criteria>
- At least one image in app/page.tsx has priority={true}
- Hero image has descriptive alt text (not "hero", "image", or empty)
- next/image used (not standard img tag)
  </acceptance_criteria>
  <verify>
grep -n "priority={true}" app/page.tsx
grep -n "alt=" app/page.tsx | head -20
  </verify>
  <done>Hero image optimized for LCP with priority loading</done>
</task>

<task type="auto">
  <name>Task 3: Verify next/image usage in Services page</name>
  <files>app/services/page.tsx</files>
  <read_first>app/services/page.tsx (read current image usage)</read_first>
  <action>
In app/services/page.tsx:
1. Ensure all images use next/image component (not <img>)
2. All next/image components must have explicit width and height props
3. Add priority={true} to any above-the-fold images
4. Ensure all images have descriptive alt text with target keywords where appropriate

If images are missing width/height:
- Either add explicit dimensions
- Or use fill prop with parent container styling
  </action>
  <acceptance_criteria>
- All images in app/services/page.tsx use next/image
- All next/image have width/height or fill prop
- No standard <img> tags present
- All images have alt text
  </acceptance_criteria>
  <verify>
grep -n "<img" app/services/page.tsx
grep -n "next/image" app/services/page.tsx
  </verify>
  <done>Services page images optimized with next/image and proper dimensions</done>
</task>

<task type="auto">
  <name>Task 4: Add local SEO keywords to existing page metadata</name>
  <files>app/page.tsx, app/about/page.tsx</files>
  <read_first>app/page.tsx (read current metadata), app/about/page.tsx (read current metadata)</read_first>
  <action>
Update metadata to include more Adelaide-specific keywords:

For app/page.tsx (if not already in 06-01):
- keywords: Add "adelaide china sourcing", "south australia sourcing", "australian import china"
- openGraph: Add locale: "en-AU", alternateLocale: "en-US"

For app/about/page.tsx:
- keywords: Ensure includes "adelaide", "south australia", "australian business", "adelaide sourcing company"
- description: Ensure mentions Adelaide location prominently
  </action>
  <acceptance_criteria>
- Homepage metadata includes "adelaide" keyword
- About page metadata includes "Adelaide" in keywords
- openGraph includes locale: "en-AU" on homepage
  </acceptance_criteria>
  <verify>
grep -n "adelaide" app/page.tsx
grep -n "adelaide" app/about/page.tsx
  </verify>
  <done>Local SEO keywords added to page metadata for Adelaide targeting</done>
</task>

</tasks>

<verification>
- LocalBusiness schema includes Adelaide address
- Hero images have priority={true}
- All images use next/image with proper dimensions
- Local keywords present in page metadata
</verification>

<success_criteria>
- LocalBusiness schema enhanced with Adelaide address and Australian coverage
- At least one hero image has priority={true} for LCP optimization
- All images in main pages use next/image with explicit dimensions
- Local keywords (adelaide, south australia) appear in metadata
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo-optimization/06-03-SUMMARY.md`
</output>
