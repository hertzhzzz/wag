---
phase: 10-content-strategy
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/page.tsx
  - app/page.tsx
autonomous: true
requirements:
  - CONT-05
user_setup: []

must_haves:
  truths:
    - "Services page metadata contains target keywords: factory visit, supplier sourcing, China procurement"
    - "Homepage metadata contains target keywords"
    - "Target keywords appear naturally in service page content"
    - "No duplicate keywords in metadata (case-insensitive dedup)"
    - "Services page has complete OpenGraph metadata (locale, alternateLocale, siteName)"
  artifacts:
    - path: "app/services/page.tsx"
      provides: "Optimized services page"
      contains: "metadata.keywords with factory visit, supplier sourcing, China procurement"
      contains: "OpenGraph with locale, alternateLocale, siteName"
    - path: "app/page.tsx"
      provides: "Optimized homepage"
      contains: "metadata.keywords with target keywords"
  key_links:
    - from: "app/services/page.tsx"
      to: "metadata"
      via: "keywords array"
      pattern: "keywords:.*factory visit"

# IMPORTANT: Keyword deduplication
# - Current services page may have 'china procurement' (lowercase)
# - Adding 'China procurement' (capitalized) creates duplicate
# - Must remove duplicates before adding new keywords

# CRITICAL: OpenGraph completeness
# - Services page needs: locale, alternateLocale, siteName
# - Homepage already has these fields (use as reference)
# - Missing fields cause incomplete social sharing cards
---

<objective>
Optimize service pages and homepage with target keywords: "factory visit", "supplier sourcing", "China procurement". Update metadata keywords array and add keyword-rich content sections where appropriate. Ensure no duplicate keywords (case-insensitive). Also add complete OpenGraph metadata to services page.
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mark/Projects/wag/app/services/page.tsx
@/Users/mark/Projects/wag/app/page.tsx

# Current service page metadata:
export const metadata: Metadata = {
  title: 'China Sourcing Services | Factory Tours & Supplier Verification',
  description: 'Our China sourcing services include factory tours...',
  keywords: ['china sourcing services', 'factory tour china', 'supplier verification china', 'china procurement', 'quality inspection china'],
}

# Homepage OpenGraph (reference for completeness):
export const metadata: Metadata = {
  openGraph: {
    title: '...',
    description: '...',
    url: 'https://www.winningadventure.com.au',
    siteName: 'Winning Adventure Global',
    locale: 'en_AU',
    alternateLocale: 'en_US',
  }
}

# Target keywords from CONTEXT.md:
- "factory visit"
- "supplier sourcing"
- "China procurement"

# IMPORTANT - Keyword deduplication:
# Current keywords include: 'china procurement' (lowercase)
# If we add 'China procurement' (capitalized), it creates a duplicate
# Must remove 'china procurement' before adding 'China procurement'
# Use case-insensitive deduplication

# CRITICAL - OpenGraph completeness:
# Services page needs: locale, alternateLocale, siteName
# Check app/page.tsx for the exact format

# Pages to optimize:
- /services (main services page)
- / (homepage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize Services page metadata with target keywords</name>
  <files>app/services/page.tsx</files>
  <read_first>
    - app/services/page.tsx (lines 1-21 with metadata)
  </read_first>
  <action>
    Update the metadata.keywords array in app/services/page.tsx to include target keywords.

    IMPORTANT - Keyword deduplication:
    1. First, check current keywords: ['china sourcing services', 'factory tour china', 'supplier verification china', 'china procurement', 'quality inspection china']
    2. Remove 'china procurement' (lowercase) before adding 'China procurement'
    3. Add these target keywords (no duplicates):
       - "factory visit"
       - "supplier sourcing"
       - "China procurement" (replace 'china procurement', don't add as duplicate)

    New keywords array should be:
    ['factory visit', 'supplier sourcing', 'China procurement', 'china sourcing services', 'factory tour china', 'supplier verification china', 'quality inspection china']

    Note: Only add "China procurement" once - remove existing lowercase version first
  </action>
  <verify>
    <automated>
      # Check target keywords exist
      grep -q "factory visit" app/services/page.tsx && \
      grep -q "supplier sourcing" app/services/page.tsx && \
      grep -q "China procurement" app/services/page.tsx && \
      # CRITICAL: Check no duplicate keywords (case-insensitive)
      # If 'china procurement' exists alongside 'China procurement', this will fail
      ! grep -qi "china procurement" app/services/page.tsx | grep -qv "China procurement" || true
    </automated>
  </verify>
  <done>
    Services page metadata contains:
    - "factory visit"
    - "supplier sourcing"
    - "China procurement" (only once, not duplicated)
    - No duplicate keywords (case-insensitive)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add complete OpenGraph metadata to Services page</name>
  <files>app/services/page.tsx</files>
  <read_first>
    - app/services/page.tsx (full file with existing OpenGraph)
    - app/page.tsx (reference for OpenGraph structure)
  </read_first>
  <action>
    Add complete OpenGraph metadata to Services page. Check app/page.tsx for the exact format and add missing fields:

    Current Services page OpenGraph should have:
    - openGraph.title
    - openGraph.description
    - openGraph.url
    - openGraph.siteName: 'Winning Adventure Global'
    - openGraph.locale: 'en_AU'
    - openGraph.alternateLocale: 'en_US'

    Reference from homepage (app/page.tsx):
    ```typescript
    openGraph: {
      title: 'China Sourcing Services | Factory Tours & Supplier Verification',
      description: 'Our China sourcing services include factory tours...',
      url: 'https://www.winningadventure.com.au/services',
      siteName: 'Winning Adventure Global',
      locale: 'en_AU',
      alternateLocale: 'en_US',
    }
    ```

    CRITICAL: Ensure these fields are present:
    - locale: 'en_AU'
    - alternateLocale: 'en_US'
    - siteName: 'Winning Adventure Global'
  </action>
  <verify>
    <automated>
      # Check OpenGraph completeness
      grep -q "locale:" app/services/page.tsx && \
      grep -q "alternateLocale:" app/services/page.tsx && \
      grep -q "siteName:" app/services/page.tsx
    </automated>
  </verify>
  <done>
    Services page OpenGraph includes:
    - locale: 'en_AU'
    - alternateLocale: 'en_US'
    - siteName: 'Winning Adventure Global'
  </done>
</task>

<task type="auto">
  <name>Task 3: Optimize Homepage metadata with target keywords</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (lines 1-30 with metadata)
  </read_first>
  <action>
    Update the metadata.keywords array in app/page.tsx to include target keywords.

    Read current keywords from homepage metadata and add:
    - "factory visit"
    - "supplier sourcing"
    - "China procurement"

    IMPORTANT - Keyword deduplication:
    - Check if 'china procurement' or 'China procurement' already exists
    - Remove duplicates before adding new keywords

    The homepage should target brand + primary commercial keywords:
    - "Winning Adventure Global"
    - "China sourcing"
    - "factory visit" (NEW)
    - "supplier sourcing" (NEW)
    - "China procurement" (NEW)
  </action>
  <verify>
    <automated>
      grep -q "factory visit" app/page.tsx && \
      grep -q "supplier sourcing" app/page.tsx && \
      grep -q "China procurement" app/page.tsx
    </automated>
  </verify>
  <done>
    Homepage metadata contains:
    - "factory visit"
    - "supplier sourcing"
    - "China procurement"
    - No duplicate keywords
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify keyword placement in service page content</name>
  <files>app/services/page.tsx</files>
  <read_first>
    - app/services/page.tsx (full file for content)
  </read_first>
  <action>
    Verify that service page content includes target keywords naturally in headings and body text.

    Target keywords to appear in content:
    - "factory visit" - should appear in hero, card descriptions, or process steps
    - "supplier sourcing" - should appear in service descriptions
    - "China procurement" - should appear in service descriptions or CTA

    If keywords are missing from content, add them naturally to:
    - Hero section (h1 or description)
    - Service card descriptions
    - Process steps

    Use existing tone: professional, direct, B2B-focused
  </action>
  <verify>
    <automated>
      grep -c "factory visit\|factory visits" app/services/page.tsx | awk '{if ($1 >= 1) exit 0; else exit 1}' || true
      grep -c "supplier sourcing" app/services/page.tsx | awk '{if ($1 >= 1) exit 0; else exit 1}' || true
      grep -c "China procurement" app/services/page.tsx | awk '{if ($1 >= 1) exit 0; else exit 1}' || true
    </automated>
  </verify>
  <done>
    Service page content includes target keywords naturally in headings or descriptions
  </done>
</task>

</tasks>

<verification>
[Overall checks]
- Services page has target keywords in metadata
- Homepage has target keywords in metadata
- Target keywords appear in service page content
- Build passes: npm run build
- Lint passes: npm run lint

[Keyword Deduplication Verification]
- Check for duplicate keywords in services page metadata:
  - 'china procurement' should NOT exist alongside 'China procurement'
  - Use case-insensitive grep to verify: grep -i "china procurement" should return only 1 match
- Check for duplicate keywords in homepage metadata
- No case variations of the same keyword should appear

[OpenGraph Completeness Verification]
- Services page must have:
  - locale: 'en_AU'
  - alternateLocale: 'en_US'
  - siteName: 'Winning Adventure Global'
- Verify with: grep -E "locale:|alternateLocale:|siteName:" app/services/page.tsx

[Build Verification]
- npm run build passes without errors
- Pages render correctly with updated metadata
</verification>

<success_criteria>
Service pages contain target keywords in headings and content
No duplicate keywords in any metadata (case-insensitive)
Services page has complete OpenGraph metadata (locale, alternateLocale, siteName)
</success_criteria>

<output>
After completion, create `.planning/phases/10-content-strategy/10-03-SUMMARY.md`
</output>
