---
phase: 10-content-strategy
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/page.tsx
  - app/page.tsx
autonomous: true
requirements:
  - CONT-05
user_setup: []

must_haves:
  truths:
    - "Services page metadata contains target keywords: factory visit, supplier sourcing, China procurement"
    - "Homepage metadata contains target keywords"
    - "Target keywords appear naturally in service page content"
  artifacts:
    - path: "app/services/page.tsx"
      provides: "Optimized services page"
      contains: "metadata.keywords with factory visit, supplier sourcing, China procurement"
    - path: "app/page.tsx"
      provides: "Optimized homepage"
      contains: "metadata.keywords with target keywords"
  key_links:
    - from: "app/services/page.tsx"
      to: "metadata"
      via: "keywords array"
      pattern: "keywords:.*factory visit"
---

<objective>
Optimize service pages and homepage with target keywords: "factory visit", "supplier sourcing", "China procurement". Update metadata keywords array and add keyword-rich content sections where appropriate.
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mark/Projects/wag/app/services/page.tsx
@/Users/mark/Projects/wag/app/page.tsx

# Current service page metadata:
export const metadata: Metadata = {
  title: 'China Sourcing Services | Factory Tours & Supplier Verification',
  description: 'Our China sourcing services include factory tours...',
  keywords: ['china sourcing services', 'factory tour china', 'supplier verification china', 'china procurement', 'quality inspection china'],
}

# Target keywords from CONTEXT.md:
- "factory visit"
- "supplier sourcing"
- "China procurement"

# Pages to optimize:
- /services (main services page)
- / (homepage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize Services page metadata with target keywords</name>
  <files>app/services/page.tsx</files>
  <read_first>
    - app/services/page.tsx (lines 1-21 with metadata)
  </read_first>
  <action>
    Update the metadata.keywords array in app/services/page.tsx to include target keywords:

    Current keywords:
    ['china sourcing services', 'factory tour china', 'supplier verification china', 'china procurement', 'quality inspection china']

    Add these target keywords:
    - "factory visit"
    - "supplier sourcing"
    - "China procurement"

    New keywords array should be:
    ['factory visit', 'supplier sourcing', 'China procurement', 'china sourcing services', 'factory tour china', 'supplier verification china', 'china procurement', 'quality inspection china']

    Note: "China procurement" already exists, but add the exact variations "factory visit" and "supplier sourcing"
  </action>
  <verify>
    <automated>
      grep -q "factory visit" app/services/page.tsx && \
      grep -q "supplier sourcing" app/services/page.tsx && \
      grep -q "China procurement" app/services/page.tsx
    </automated>
  </verify>
  <done>
    Services page metadata contains:
    - "factory visit"
    - "supplier sourcing"
    - "China procurement"
  </done>
</task>

<task type="auto">
  <name>Task 2: Optimize Homepage metadata with target keywords</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (lines 1-30 with metadata)
  </read_first>
  <action>
    Update the metadata.keywords array in app/page.tsx to include target keywords.

    Read current keywords from homepage metadata and add:
    - "factory visit"
    - "supplier sourcing"
    - "China procurement"

    The homepage should target brand + primary commercial keywords:
    - "Winning Adventure Global"
    - "China sourcing"
    - "factory visit" (NEW)
    - "supplier sourcing" (NEW)
    - "China procurement" (NEW)
  </action>
  <verify>
    <automated>
      grep -q "factory visit" app/page.tsx && \
      grep -q "supplier sourcing" app/page.tsx && \
      grep -q "China procurement" app/page.tsx
    </automated>
  </verify>
  <done>
    Homepage metadata contains:
    - "factory visit"
    - "supplier sourcing"
    - "China procurement"
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify keyword placement in service page content</name>
  <files>app/services/page.tsx</files>
  <read_first>
    - app/services/page.tsx (full file for content)
  </read_first>
  <action>
    Verify that service page content includes target keywords naturally in headings and body text.

    Target keywords to appear in content:
    - "factory visit" - should appear in hero, card descriptions, or process steps
    - "supplier sourcing" - should appear in service descriptions
    - "China procurement" - should appear in service descriptions or CTA

    If keywords are missing from content, add them naturally to:
    - Hero section (h1 or description)
    - Service card descriptions
    - Process steps

    Use existing tone: professional, direct, B2B-focused
  </action>
  <verify>
    <automated>
      grep -c "factory visit\|factory visits" app/services/page.tsx | awk '{if ($1 >= 1) exit 0; else exit 1}' || true
      grep -c "supplier sourcing" app/services/page.tsx | awk '{if ($1 >= 1) exit 0; else exit 1}' || true
      grep -c "China procurement" app/services/page.tsx | awk '{if ($1 >= 1) exit 0; else exit 1}' || true
    </automated>
  </verify>
  <done>
    Service page content includes target keywords naturally in headings or descriptions
  </done>
</task>

</tasks>

<verification>
[Overall checks]
- Services page has target keywords in metadata
- Homepage has target keywords in metadata
- Target keywords appear in service page content
- Build passes: npm run build
- Lint passes: npm run lint
</verification>

<success_criteria>
Service pages contain target keywords in headings and content
</success_criteria>

<output>
After completion, create `.planning/phases/10-content-strategy/10-03-SUMMARY.md`
</output>
