---
phase: 08-security-audit
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - pnpm-lock.yaml
autonomous: true
gap_closure: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "npm audit returns no critical or high vulnerabilities"
    - "browser-use removed (unused dependency with vulnerabilities)"
    - "eslint-config-next updated to version 15+"
    - "Next.js upgraded to version 16.x"
    - "Build passes with updated dependencies"
  artifacts:
    - path: "package.json"
      provides: "Updated dependencies with secure versions"
      contains: "next@^16"
      not_contains: "browser-use"
    - path: "package-lock.json"
      provides: "Locked dependency versions"
  key_links:
    - from: "package.json"
      to: "npm audit"
      via: "npm install"
      pattern: "npm audit.*passes"
---

<objective>
Close the npm audit vulnerability gap by removing unused browser-use package, upgrading eslint-config-next to 15+, and upgrading Next.js to version 16.x as requested by user. These changes will fix the 21 high severity vulnerabilities from transitive dependencies.
</objective>

<context>
@package.json
@.planning/phases/08-security-audit-analyze-and-fix-all-security-risks-in-the-wag-website/08-VERIFICATION.md (gap details)
@.planning/phases/08-security-audit-analyze-and-fix-all-security-risks-in-the-wag-website/08-01-SUMMARY.md (previous work)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused browser-use dependency</name>
  <files>package.json, package-lock.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (line 19: browser-use@^0.5.0)
    - Verify browser-use is not imported anywhere in codebase (grep confirmed no imports)
  </read_first>
  <action>
    Remove browser-use from package.json as it is not used in the codebase and contains vulnerable transitive dependencies (fast-xml-parser, AWS SDK chain). Execute:
    ```
    npm uninstall browser-use
    ```
    Or manually remove "browser-use": "^0.5.0" from dependencies section.
  </action>
  <acceptance_criteria>
    - browser-use no longer appears in package.json dependencies
    - npm install completes successfully
  </acceptance_criteria>
  <verify>
    <automated>grep -c '"browser-use"' package.json || echo "not found"</automated>
  </verify>
  <done>browser-use removed from dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade eslint-config-next to version 15+</name>
  <files>package.json, package-lock.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (line 48: eslint-config-next@^14.2.0)
  </read_first>
  <action>
    Upgrade eslint-config-next from ^14.2.0 to ^15.0.0 to fix glob vulnerability. Execute:
    ```
    npm install eslint-config-next@^15.0.0
    ```
    Version 15+ uses a patched version of glob without the command injection vulnerability.
  </action>
  <acceptance_criteria>
    - package.json shows eslint-config-next@^15.0.0 or later
    - npm install completes successfully
    - npm run lint passes without errors
  </acceptance_criteria>
  <verify>
    <automated>grep '"eslint-config-next":' package.json</automated>
  </verify>
  <done>eslint-config-next upgraded to 15+, lint passes</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade Next.js to version 16.x (requires React 19)</name>
  <files>package.json, package-lock.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (line 25: next@^14.2.35, line 28: react@^18, line 29: react-dom@^18)
    - Check Next.js 16 release notes for breaking changes
  </read_first>
  <action>
    Upgrade Next.js from 14.2.x to 16.x and React from 18 to 19. Next.js 16 requires React 19. Execute:
    ```
    npm install next@^16.0.0 react@^19 react-dom@^19
    ```
    Also upgrade related packages that may need React 19 compatibility:
    - next-mdx-remote
    - zod
    - resend
  </action>
  <acceptance_criteria>
    - package.json shows next@^16.0.0 or later
    - package.json shows react@^19 and react-dom@^19
    - npm install completes successfully
    - npm run build passes (CRITICAL)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Next.js 16.x and React 19 installed, build passes</done>
</task>

<task type="auto">
  <name>Task 4: Verify npm audit passes with no high/critical vulnerabilities</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - Verify previous tasks completed successfully
  </read_first>
  <action>
    Run npm audit to verify all vulnerabilities are resolved. Execute:
    ```
    npm audit
    ```
    The previously found 21 high severity vulnerabilities from browser-use (fast-xml-parser) and eslint-config-next (glob) should now be gone.
  </action>
  <acceptance_criteria>
    - npm audit returns exit code 0
    - Output shows 0 critical vulnerabilities
    - Output shows 0 high vulnerabilities
  </acceptance_criteria>
  <verify>
    <automated>npm audit --audit-level=high</automated>
  </verify>
  <done>npm audit passes with no high or critical vulnerabilities</done>
</task>

</tasks>

<verification>
- npm audit --audit-level=high returns 0 vulnerabilities
- npm run build passes
- npm run lint passes
- All 5 pages accessible (/, /services, /about, /resources, /enquiry)
</verification>

<success_criteria>
1. npm audit shows no critical or high vulnerabilities (gap closed)
2. browser-use removed (unused dependency eliminated)
3. eslint-config-next upgraded to 15+
4. Next.js upgraded to 16.x with React 19
5. Build passes with updated dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-audit-analyze-and-fix-all-security-risks-in-the-wag-website/08-05-SUMMARY.md`
</output>
