---
phase: 08-security-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.js
  - app/api/enquiry/route.ts
  - app/api/newsletter/route.ts
autonomous: true
requirements:
  - SEC-03
  - SEC-04
must_haves:
  truths:
    - "Security headers are present in HTTP responses (HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy)"
    - "API routes only accept requests from allowed origins (winningadventure.com.au)"
    - "CORS preflight (OPTIONS) requests are handled properly"
  artifacts:
    - path: "next.config.js"
      provides: "Security headers configuration"
      contains: "Strict-Transport-Security"
    - path: "app/api/enquiry/route.ts"
      provides: "CORS-enabled enquiry API"
      contains: "ALLOWED_ORIGINS"
    - path: "app/api/newsletter/route.ts"
      provides: "CORS-enabled newsletter API"
      contains: "ALLOWED_ORIGINS"
  key_links:
    - from: "next.config.js"
      to: "HTTP responses"
      via: "headers() function"
      pattern: "security headers present"
---

<objective>
Add security headers to the Next.js application and configure CORS for API routes. Security headers include HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, and Permissions-Policy. CORS configuration restricts API access to allowed origins only.
</objective>

<context>
@next.config.js
@app/api/enquiry/route.ts
@app/api/newsletter/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security headers to next.config.js</name>
  <files>next.config.js</files>
  <read_first>
    - next.config.js (current configuration without security headers)
  </read_first>
  <action>
    Add security headers to next.config.js. Modify the config to include:
    ```javascript
    async headers() {
      return [
        {
          source: '/:path*',
          headers: [
            { key: 'Strict-Transport-Security', value: 'max-age=31536000; includeSubDomains' },
            { key: 'X-Frame-Options', value: 'DENY' },
            { key: 'X-Content-Type-Options', value: 'nosniff' },
            { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
            { key: 'Permissions-Policy', value: 'camera=(), microphone=(), geolocation=()' }
          ]
        }
      ]
    }
    ```
    These headers prevent XSS, clickjacking, MIME sniffing, and control browser features.
  </action>
  <acceptance_criteria>
    - next.config.js contains async headers() function
    - Headers include: Strict-Transport-Security, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy
    - npm run build passes
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Security headers configured in next.config.js</done>
</task>

<task type="auto">
  <name>Task 2: Add CORS configuration to enquiry API route</name>
  <files>app/api/enquiry/route.ts</files>
  <read_first>
    - app/api/enquiry/route.ts (current implementation without CORS)
  </read_first>
  <action>
    Add CORS configuration to the enquiry API route. The allowed origins should be:
    - https://www.winningadventure.com.au
    - https://winningadventure.com.au

    Add an ALLOWED_ORIGINS constant at the top of the file:
    ```typescript
    const ALLOWED_ORIGINS = [
      'https://www.winningadventure.com.au',
      'https://winningadventure.com.au'
    ]
    ```

    Modify the POST handler to check origin and add CORS headers to response. Add OPTIONS handler for preflight requests.
  </action>
  <acceptance_criteria>
    - ALLOWED_ORIGINS array defined with both domain variants
    - POST handler checks origin and returns 403 if not allowed
    - Response includes Access-Control-Allow-Origin header
    - OPTIONS handler exists for CORS preflight
  </acceptance_criteria>
  <verify>
    <automated>grep -n "ALLOWED_ORIGINS" app/api/enquiry/route.ts && grep -n "OPTIONS" app/api/enquiry/route.ts</automated>
  </verify>
  <done>CORS configured for enquiry API route</done>
</task>

<task type="auto">
  <name>Task 3: Add CORS configuration to newsletter API route</name>
  <files>app/api/newsletter/route.ts</files>
  <read_first>
    - app/api/newsletter/route.ts (current implementation without CORS)
  </read_first>
  <action>
    Add CORS configuration to the newsletter API route using the same pattern as enquiry route. The allowed origins should be:
    - https://www.winningadventure.com.au
    - https://winningadventure.com.au

    Add ALLOWED_ORIGINS constant, modify POST handler to check origin, add CORS headers to response, and add OPTIONS handler.
  </action>
  <acceptance_criteria>
    - ALLOWED_ORIGINS array defined with both domain variants
    - POST handler checks origin and returns 403 if not allowed
    - Response includes Access-Control-Allow-Origin header
    - OPTIONS handler exists for CORS preflight
  </acceptance_criteria>
  <verify>
    <automated>grep -n "ALLOWED_ORIGINS" app/api/newsletter/route.ts && grep -n "OPTIONS" app/api/newsletter/route.ts</automated>
  </verify>
  <done>CORS configured for newsletter API route</done>
</task>

</tasks>

<verification>
- npm run build passes with new configuration
- curl -I localhost:3000 shows security headers in response (after deploy)
- Manual test: API accepts requests from allowed origins, rejects others
</verification>

<success_criteria>
1. Security headers present in next.config.js
2. CORS configured for enquiry API route
3. CORS configured for newsletter API route
4. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-audit/08-02-SUMMARY.md`
</output>
