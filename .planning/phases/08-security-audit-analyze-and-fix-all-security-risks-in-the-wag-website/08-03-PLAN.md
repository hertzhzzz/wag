---
phase: 08-security-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/rate-limit.ts
  - app/api/enquiry/route.ts
  - app/api/newsletter/route.ts
autonomous: true
requirements:
  - SEC-05
  - SEC-06
user_setup:
  - service: upstash
    why: "Rate limiting requires Redis for persistent storage"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Console -> Database Details"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Console -> Database Details"
must_haves:
  truths:
    - "Rate limiting uses persistent Redis storage (survives server restarts)"
    - "Enquiry API limits to 3 requests per minute per IP"
    - "Newsletter API limits to 3 requests per minute per IP"
    - "Blocked requests return 429 status code"
  artifacts:
    - path: "lib/rate-limit.ts"
      provides: "Rate limiting configuration with upstash"
      contains: "Ratelimit"
    - path: "app/api/enquiry/route.ts"
      provides: "Rate-limited enquiry API"
      contains: "ratelimit"
    - path: "app/api/newsletter/route.ts"
      provides: "Rate-limited newsletter API"
      contains: "ratelimit"
  key_links:
    - from: "lib/rate-limit.ts"
      to: "API routes"
      via: "import"
      pattern: "ratelimit.limit"
---

<objective>
Implement persistent rate limiting using upstash/ratelimit-js for the enquiry and newsletter API routes. This replaces any existing in-memory rate limiting with a Redis-backed solution that survives server restarts and handles distributed requests.
</objective>

<context>
@package.json
@app/api/enquiry/route.ts
@app/api/newsletter/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install upstash/ratelimit packages</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependencies)
  </read_first>
  <action>
    Install the upstash rate limiting packages. Execute:
    ```
    npm install @upstash/ratelimit @upstash/redis
    ```
    These packages provide persistent rate limiting designed for serverless/Vercel deployment with a free tier.
  </action>
  <acceptance_criteria>
    - package.json contains @upstash/ratelimit and @upstash/redis
    - package-lock.json updated
    - npm install completes without errors
  </acceptance_criteria>
  <verify>
    <automated>grep -E "@upstash/(ratelimit|redis)" package.json</automated>
  </verify>
  <done>Rate limiting packages installed</done>
</task>

<task type="auto">
  <name>Task 2: Create rate limiting utility library</name>
  <files>lib/rate-limit.ts</files>
  <read_first>
    - app/api/enquiry/route.ts (to understand existing implementation)
  </read_first>
  <action>
    Create lib/rate-limit.ts with the upstash rate limiter configuration:
    ```typescript
    import { Ratelimit } from '@upstash/ratelimit'
    import { Redis } from '@upstash/redis'

    // Create rate limiter - 3 requests per minute per IP
    export const ratelimit = new Ratelimit({
      redis: Redis.fromEnv(),
      limiter: Ratelimit.slidingWindow(3, '60 s'),
      analytics: true
    })
    ```
    This provides persistent rate limiting that survives server restarts.
  </action>
  <acceptance_criteria>
    - lib/rate-limit.ts exists and exports ratelimit object
    - Uses Redis.fromEnv() for configuration
    - Configures sliding window: 3 requests per 60 seconds
  </acceptance_criteria>
  <verify>
    <automated>ls -la lib/rate-limit.ts</automated>
  </verify>
  <done>Rate limiting utility library created</done>
</task>

<task type="auto">
  <name>Task 3: Add persistent rate limiting to enquiry API</name>
  <files>app/api/enquiry/route.ts</files>
  <read_first>
    - app/api/enquiry/route.ts (current implementation)
    - lib/rate-limit.ts (just created)
  </read_first>
  <action>
    Update app/api/enquiry/route.ts to use persistent rate limiting:
    1. Import ratelimit from '@/lib/rate-limit'
    2. Add IP extraction from headers (x-forwarded-for, x-real-ip)
    3. Call ratelimit.limit(ip) before processing
    4. Return 429 if rate limit exceeded

    The rate limiting should be applied before any other processing.
  </action>
  <acceptance_criteria>
    - enquiry/route.ts imports ratelimit from lib/rate-limit
    - IP is extracted from request headers
    - ratelimit.limit() is called before processing
    - Returns 429 status with error message when rate limited
  </acceptance_criteria>
  <verify>
    <automated>grep -n "ratelimit" app/api/enquiry/route.ts && grep -n "429" app/api/enquiry/route.ts</automated>
  </verify>
  <done>Enquiry API has persistent rate limiting</done>
</task>

<task type="auto">
  <name>Task 4: Add persistent rate limiting to newsletter API</name>
  <files>app/api/newsletter/route.ts</files>
  <read_first>
    - app/api/newsletter/route.ts (current implementation)
    - lib/rate-limit.ts (just created)
  </read_first>
  <action>
    Update app/api/newsletter/route.ts to use persistent rate limiting following the same pattern as enquiry API:
    1. Import ratelimit from '@/lib/rate-limit'
    2. Add IP extraction from headers
    3. Call ratelimit.limit(ip) before processing
    4. Return 429 if rate limit exceeded
  </action>
  <acceptance_criteria>
    - newsletter/route.ts imports ratelimit from lib/rate-limit
    - IP is extracted from request headers
    - ratelimit.limit() is called before processing
    - Returns 429 status with error message when rate limited
  </acceptance_criteria>
  <verify>
    <automated>grep -n "ratelimit" app/api/newsletter/route.ts && grep -n "429" app/api/newsletter/route.ts</automated>
  </verify>
  <done>Newsletter API has persistent rate limiting</done>
</task>

</tasks>

<verification>
- npm run build passes with new dependencies
- Rate limiting configuration is properly imported in both API routes
- Environment variables documented for Vercel setup
</verification>

<success_criteria>
1. upstash packages installed
2. lib/rate-limit.ts created with ratelimit configuration
3. Enquiry API has persistent rate limiting
4. Newsletter API has persistent rate limiting
5. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-audit/08-03-SUMMARY.md`
</output>
