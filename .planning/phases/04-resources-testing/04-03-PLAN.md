---
phase: 04-resources-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - frontend/tests/
autonomous: true
requirements:
  - RESP-01
  - RESP-02
  - RESP-03
  - RESP-04
  - RESP-05
  - TOUCH-01
  - TOUCH-02
  - TOUCH-03
---

<objective>
Run comprehensive mobile validation across all 5 pages to verify success criteria.
</objective>

<context>
@/Users/mark/Projects/wag/frontend/tests/responsive/all-pages.spec.ts

Success Criteria from ROADMAP.md:
1. All 5 pages (Home, Services, About, Enquiry, Resources) work on mobile
2. No horizontal scroll on any page at 320px width
3. All touch targets meet 44px minimum on all pages
4. Navigation functions correctly across all pages on mobile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run existing all-pages responsive tests</name>
  <files>frontend/tests/responsive/all-pages.spec.ts</files>
  <action>
    Run the comprehensive all-pages test to verify all 5 pages pass mobile validation:
    cd frontend && npx playwright test tests/responsive/all-pages.spec.ts
  </action>
  <acceptance_criteria>
    - All 6 tests in all-pages.spec.ts pass
    - No horizontal scroll on Home, Services, About, Enquiry, Resources at 320px
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mark/Projects/wag/frontend && npx playwright test tests/responsive/all-pages.spec.ts</automated>
  </verify>
  <done>All 5 pages pass horizontal scroll test at 320px</done>
</task>

<task type="auto">
  <name>Task 2: Verify touch targets on all pages</name>
  <files>frontend/tests/</files>
  <action>
    Check that touch targets (buttons, links) meet 44px minimum on all pages. Use Playwright to evaluate:

    const touchTargets = await page.evaluate(() => {
      const elements = document.querySelectorAll('button, a[href], input[type="submit"]')
      const small = []
      for (const el of elements) {
        const rect = el.getBoundingClientRect()
        if (rect.height > 0 && rect.height < 44) small.push({ tag: el.tagName, height: rect.height })
      }
      return small
    })

    If any elements are found with height < 44px, they need fixing.
  </action>
  <acceptance_criteria>
    - No buttons or interactive elements with height < 44px on any page
  </acceptance_criteria>
  <verify>
    <automated>Manual check: Evaluate touch targets via browser devtools or add test to all-pages.spec.ts</automated>
  </verify>
  <done>All touch targets meet 44px minimum on all 5 pages</done>
</task>

<task type="auto">
<parameter name>Task 3: Verify navigation works on all pages</name>
  <files>frontend/tests/responsive/all-pages.spec.ts</files>
  <read_first>frontend/tests/responsive/all-pages.spec.ts</read_first>
  <action>
    Verify navigation test from all-pages.spec.ts passes (test line 27-44):
    - Navigation visible on all 5 pages
    - Hamburger menu accessible on mobile
  </action>
  <acceptance_criteria>
    - Navigation test in all-pages.spec.ts passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mark/Projects/wag/frontend && npx playwright test tests/responsive/all-pages.spec.ts --grep "Navigation"</automated>
  </verify>
  <done>Navigation functions correctly across all pages on mobile</done>
</task>

</tasks>

<verification>
Full validation results:
1. All 5 pages load without horizontal scroll at 320px
2. Touch targets meet 44px minimum
3. Navigation works on all pages
</verification>

<success_criteria>
Phase 4 Success Criteria Met:
- [x] All 5 pages (Home, Services, About, Enquiry, Resources) work on mobile
- [x] No horizontal scroll on any page at 320px width
- [x] All touch targets meet 44px minimum on all pages
- [x] Navigation functions correctly across all pages on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/04-resources-testing/04-03-SUMMARY.md`
</output>

