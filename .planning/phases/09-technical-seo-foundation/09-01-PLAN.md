---
phase: 09-technical-seo-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Hero.tsx
  - public/hero-image.webp
autonomous: false
requirements:
  - TECH-01
  - TECH-02
  - TECH-03
  - TECH-04
  - TECH-05
  - MON-01
user_setup: []

must_haves:
  truths:
    - "Mobile LCP performance measures less than 2.5 seconds in PageSpeed Insights"
    - "XML sitemap accessible at /sitemap.xml with all 5 main pages listed"
    - "robots.txt configured with proper allow rules and sitemap reference"
    - "Schema.org Organization + LocalBusiness JSON-LD present in page source"
    - "Canonical URLs present in HTML head of all 5 main pages"
    - "Google Search Console property shows site as indexed and verified"
  artifacts:
    - path: "app/sitemap.ts"
      provides: "Dynamic sitemap generation for all pages"
      contains: "priority 1 for homepage, priority 0.9 for services"
    - path: "public/robots.txt"
      provides: "Robots configuration with sitemap reference"
      contains: "Allow: /, Sitemap: https://www.winningadventure.com.au/sitemap.xml"
    - path: "app/layout.tsx"
      provides: "Schema.org markup, canonical URLs, GSC verification"
      contains: "@type Organization, @type LocalBusiness, canonical URL"
    - path: "app/components/Hero.tsx"
      provides: "LCP-optimized hero with local WebP image"
      contains: "src=\"/hero-image.webp\", priority={true}"
    - path: "public/hero-image.webp"
      provides: "Local WebP image for fast LCP"
      contains: "WebP binary image file"
  key_links:
    - from: "public/robots.txt"
      to: "app/sitemap.ts"
      via: "Sitemap directive"
      pattern: "Sitemap:.*sitemap.xml"
    - from: "app/layout.tsx"
      to: "Schema.org validators"
      via: "JSON-LD in head"
      pattern: "application/ld\\+json"
    - from: "app/components/Hero.tsx"
      to: "public/hero-image.webp"
      via: "next/image component"
      pattern: "src=\"/hero-image.webp\""
---

<objective>
Fix LCP performance and verify all technical SEO infrastructure is working correctly.

Purpose: The WAG website currently has 5.4s LCP on mobile (target <2.5s). Most SEO infrastructure already exists (sitemap, robots.txt, Schema.org, canonicals, GSC). This plan verifies existing infrastructure and fixes the LCP bottleneck through hero image optimization.

Output: All technical SEO requirements satisfied, mobile LCP <2.5s
</objective>

<context>
@/Users/mark/Projects/wag/app/layout.tsx
@/Users/mark/Projects/wag/app/sitemap.ts
@/Users/mark/Projects/wag/public/robots.txt
@/Users/mark/Projects/wag/app/components/Hero.tsx

# Existing SEO Infrastructure Status

1. **Sitemap** (app/sitemap.ts) - Dynamic generation with blog posts
2. **robots.txt** (public/robots.txt) - Basic allow all, sitemap reference
3. **Schema.org** (layout.tsx lines 106-185) - Organization + LocalBusiness JSON-LD
4. **Canonical URLs** (layout.tsx line 64) - Set in metadata.alternates
5. **GSC** (layout.tsx line 70) - Verification code present

# LCP Issue
- Hero image uses external Unsplash URL: `https://images.unsplash.com/photo-1581094794329-c8112a89af12?w=800&q=80`
- Has `priority={true}` but external CDN still causes DNS/connection delay
- Solution: Download image locally, convert to WebP, use next/image with local path
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify existing SEO infrastructure</name>
  <files>app/sitemap.ts, public/robots.txt, app/layout.tsx</files>
  <read_first>
    - app/sitemap.ts (verify dynamic sitemap with all 5 pages + blog)
    - public/robots.txt (verify sitemap reference)
    - app/layout.tsx (verify Schema.org JSON-LD for Organization + LocalBusiness, canonical URL, GSC verification)
  </read_first>
  <action>
    Verify all existing SEO infrastructure is correctly configured:

    1. **Sitemap** (TECH-02): Confirm app/sitemap.ts generates URLs for:
       - / (priority 1)
       - /services (priority 0.9)
       - /about (priority 0.7)
       - /resources (priority 0.8)
       - /enquiry (priority 0.8)
       - Blog posts with priority 0.8

    2. **robots.txt** (TECH-03): Confirm public/robots.txt contains:
       - "User-agent: *"
       - "Allow: /"
       - Sitemap reference to https://www.winningadventure.com.au/sitemap.xml

    3. **Schema.org** (TECH-04): Confirm layout.tsx contains both:
       - Organization schema with name, url, logo, founder, address, telephone, ABN
       - LocalBusiness schema with name, image, telephone, address, geo coordinates, opening hours

    4. **Canonical URLs** (TECH-05): Confirm metadata.alternates.canonical = "https://www.winningadventure.com.au"

    5. **GSC** (MON-01): Confirm metadata.verification.google = "G-VEGJ1YL8YR"
  </action>
  <verify>
    <automated>
      grep -q "priority: 1" app/sitemap.ts && \
      grep -q "Sitemap: https://www.winningadventure.com.au/sitemap.xml" public/robots.txt && \
      grep -q '"@type": "Organization"' app/layout.tsx && \
      grep -q '"@type": "LocalBusiness"' app/layout.tsx && \
      grep -q 'canonical: '\''https://www.winningadventure.com.au'\''' app/layout.tsx && \
      grep -q 'google: '\''G-VEGJ1YL8YR'\''' app/layout.tsx && echo "VERIFIED"
    </automated>
  </verify>
  <done>
    All 5 SEO infrastructure elements verified and working:
    - Dynamic sitemap generates all required URLs
    - robots.txt allows all and references sitemap
    - Organization + LocalBusiness schema present in page source
    - Canonical URL configured for default language
    - Google Search Console verification code present
  </done>
</task>

<task type="auto">
  <name>Task 2: Optimize LCP - Hero image local WebP</name>
  <files>app/components/Hero.tsx, public/hero-image.webp</files>
  <read_first>
    - app/components/Hero.tsx (current implementation with external Unsplash URL)
    - next.config.js (verify image domains configuration if needed)
  </read_first>
  <action>
    Fix LCP performance bottleneck by using local WebP image instead of external Unsplash:

    1. Download hero image from Unsplash:
       Source: https://images.unsplash.com/photo-1581094794329-c8112a89af12?w=1200&q=80
       Save to: public/hero-image.webp (convert to WebP format)

    2. Update Hero.tsx to use local image:
       - Change src from "https://images.unsplash.com/..." to "/hero-image.webp"
       - Keep priority={true} and fetchPriority="high"
       - Update sizes to "(max-width: 768px) 100vw, 1200px"
       - Remove external URL dependency for mobile LCP

    3. Ensure next/image is properly configured for local images:
       - Use local path: src="/hero-image.webp"
       - Remove width/height when using fill (already using fill)
       - Keep alt text descriptive

    This targets TECH-01: Mobile LCP 5.4s -> under 2.5s
  </action>
  <verify>
    <automated>
      ls -la public/hero-image.webp && \
      grep -q 'src="/hero-image.webp"' app/components/Hero.tsx && \
      grep -q 'priority={true}' app/components/Hero.tsx && \
      grep -q 'fetchPriority="high"' app/components/Hero.tsx && \
      ! grep -q "images.unsplash.com" app/components/Hero.tsx && echo "LCP OPTIMIZED"
    </automated>
  </verify>
  <done>
    LCP optimization complete:
    - Local WebP hero image exists at public/hero-image.webp
    - Hero.tsx uses local image path with priority hints
    - No external Unsplash URL in Hero component
    - Ready for PageSpeed verification: mobile LCP should measure <2.5s
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all SEO infrastructure + LCP performance</name>
  <files>N/A - manual verification</files>
  <action>
    Perform manual verification of all SEO infrastructure after deployment:
    1. Deploy changes to production (git push)
    2. Verify sitemap at /sitemap.xml
    3. Verify robots.txt at /robots.txt
    4. Verify Schema.org markup in page source
    5. Verify canonical URLs in page source
    6. Verify GSC property status
    7. Run PageSpeed Insights mobile test for LCP <2.5s
  </action>
  <what-to-verify>
    <steps>
      <step>
        **Sitemap**: Visit https://www.winningadventure.com.au/sitemap.xml
        - Should return XML with all 5 pages + blog posts
        - Look for <url> entries for /, /services, /about, /resources, /enquiry
      </step>
      <step>
        **robots.txt**: Visit https://www.winningadventure.com.au/robots.txt
        - Should show "Allow: /" and sitemap reference
      </step>
      <step>
        **Schema.org**: Visit homepage, view page source
        - Search for "application/ld+json"
        - Should see both Organization and LocalBusiness types
      </step>
      <step>
        **Canonical**: View page source of any page
        - Search for "canonical" link tag
        - Should point to correct page URL
      </step>
      <step>
        **GSC**: Log into Google Search Console
        - Property "www.winningadventure.com.au" should show as verified
        - Should show indexing status
      </step>
      <step>
        **LCP Performance**: Run PageSpeed Insights on mobile
        - URL: https://pagespeed.web.dev/
        - Enter: https://www.winningadventure.com.au
        - Select: Mobile
        - **Target**: Largest Contentful Paint < 2.5 seconds
      </step>
    </steps>
    <expected_outcomes>
      - All 5 SEO infrastructure elements verified
      - Mobile LCP measures less than 2.5 seconds
    </expected_outcomes>
  </what-to-verify>
  <verify>
    <automated>MISSING - Manual verification required via browser</automated>
  </verify>
  <done>
    All SEO infrastructure verified and LCP performance confirmed:
    - Sitemap accessible and contains all pages
    - robots.txt configured correctly
    - Schema.org markup present in page source
    - Canonical URLs present on all pages
    - GSC shows verified status
    - Mobile LCP measures <2.5 seconds
  </done>
  <resume-signal>
    Type "approved" if all verifications pass, or describe which items need fixes
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] grep confirms sitemap.ts has all 5 page URLs
- [ ] grep confirms robots.txt has sitemap reference
- [ ] grep confirms Schema.org Organization + LocalBusiness in layout.tsx
- [ ] grep confirms canonical URL in metadata
- [ ] grep confirms GSC verification code
- [ ] local hero-image.webp exists
- [ ] Hero.tsx uses local /hero-image.webp path (not Unsplash)
- [ ] npm run build succeeds
</verification>

<success_criteria>
Phase 9 complete when:
1. Mobile LCP performance measures <2.5s in PageSpeed Insights
2. XML sitemap accessible at /sitemap.xml showing all pages
3. robots.txt configured with sitemap reference
4. Schema.org Organization + LocalBusiness markup present in page source
5. Canonical URLs present on all 5 main pages
6. Google Search Console shows site as indexed and tracked
</success_criteria>

<output>
After completion, create `.planning/phases/09-technical-seo-foundation/09-01-SUMMARY.md`
</output>
