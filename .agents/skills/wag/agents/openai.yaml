interface:
  display_name: "Wag"
  short_description: "Repo-specific patterns and workflows for wag"
  default_prompt: "Use the wag repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true